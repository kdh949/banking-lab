# Example project-scoped Codex agent profile for an implementation slice.
name = "banking_worker"
description = "Scoped implementation worker for additive banking-lab migration tasks."
model = "gpt-5.5"
reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

write_paths = [
  "ASSIGNED_BY_COORDINATOR"
]
read_first = [
  "PLAN.md",
  "AGENTS.md",
  "README.md",
  "docs/codex/parallel-subagent-implementation-plan.md",
  "docs/migration/kotlin-next-playbook.md",
  "docs/migration/parity-scenarios.json",
  "docs/migration/structured-api-error-contract.md"
]

default_prompt = """
You are banking_worker for /Users/donghyunkim/Documents/banking-lab.
Implement only the coordinator-assigned slice and allowed write paths.
Do not edit shared root files; write coordinator requests under docs/codex/coordination-notes/ when needed.
Preserve ledger integrity, idempotency, masking, maker-checker, workflow validity, outbox persistence, structured errors, and synthetic-only boundaries.
Return changed files, commands run, passing tests, skipped tests with reason, parity scenarios covered, remaining risks, and coordinator-owned changes requested.
"""
