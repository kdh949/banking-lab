# Evidence Gap Report

Review date: 2026-06-04

## Verdict

Evidence is adequate for retiring the Node reference runtime from target-path dependency for the current synthetic lab scope. The current evidence proves the direct Node oracle mapping, parts of the Kotlin/PostgreSQL ledger slice, PostgreSQL audit/masking parity for reason-required staff access, append-only hash-chain continuity, default customer masking, and masked account numbers, Temporal test-environment workflow contracts, live Temporal worker smoke, Temporal worker Micrometer/Prometheus metrics, a synthetic Temporal retry drill, a live Temporal SDK worker restart drill, live Compose Temporal worker container restart drills for all current Temporal workflow case types, live Compose Temporal server container restart drills for all current Temporal workflow case types, live Compose PostgreSQL restart drills for all current Temporal workflow case types, live Compose Temporal platform host-crash-shaped restart evidence for all current Temporal workflow case types, signed JWT/JWKS authorization checks, live Keycloak realm token/MFA/WebAuthn required-action smoke, synthetic WebAuthn policy and passkey recovery role segregation, non-synthetic passkey operations through the generated manual-live-passkey artifact, customer-web interactive Keycloak propagation for all current API-backed customer paths, staff-terminal interactive Keycloak propagation for masked lookup, privileged unmask, customer-change approval, and WebAuthn virtual-authenticator completion, complaint-portal interactive Keycloak propagation for answer approval and workflow failure-state, ops-console interactive Keycloak propagation for reconciliation adjustment and workflow failure-state, audit-console interactive Keycloak propagation for hash-chain read-model evidence, FDS/AML-console interactive Keycloak propagation for risk read-model, FDS release/block approval, AML closure approval, and workflow failure-state evidence, admin-console interactive Keycloak propagation for the `security-admin01` platform-control summary, real Redpanda-backed outbox publish/consume behavior, outbox crash-before-mark-published replay/idempotency behavior, target-stack outbox worker entrypoint and Micrometer/Prometheus actuator metrics, live Compose outbox worker restart-before-publish behavior, live Compose outbox post-broker-ack crash/replay behavior, live Compose API post-commit crash/replay behavior, live Compose outbox platform host-crash-shaped recovery for durable pending events, outbox worker trace/log correlation, basic OpenTelemetry trace/log correlation for the Spring HTTP path, workflow-specific OpenTelemetry trace/log correlation for Temporal signal and approval-completion paths across all current workflow case types, live Temporal rejection/self-approval failure transition trace/log correlation, Loki-ingested Temporal workflow failure logs, a firing `TemporalWorkflowFailed` Loki ruler alert, provisioned Grafana workflow-failure dashboard validation, seven-channel API-backed read-model smoke, customer transfer retry/failure browser smoke, customer transaction history/held FDS status browser smoke, customer held/failed transfer status parity, customer complaint entry/list/confirmation target parity, Spring target parity for customer-created FDS assign/release/block, AML closure, and reconciliation adjustment, staff privileged unmask browser smoke with structured denial visibility, customer change approval command smoke, complaint answer approval command smoke, FDS release approval command smoke, FDS block approval command smoke, AML closure approval command smoke, reconciliation adjustment approval command smoke, complaint/FDS/AML/reconciliation workflow failure-state smokes from the browser, npm audit plus Docker-backed Semgrep/Trivy/SBOM generated security evidence, local Prometheus/Grafana/Loki/Tempo readiness plus Prometheus scrape evidence for core-banking, the Temporal worker, and Redpanda, and the final retirement review artifact. DAST remains skipped in the current generated security artifact because no `BANKING_LAB_DAST_URL` was supplied. It proves non-synthetic passkey operations and final Node independence for the current retirement gate.

## Proven Or Structurally Present

| Area | Current evidence | Confidence |
| --- | --- | --- |
| Node oracle | `npm test` and `npm run parity` are documented as passing in `docs/test-evidence/migration-foundation.md`. | High for reference behavior. |
| Parity scenario inventory | `docs/migration/parity-scenarios.json` maps 42 Node scenarios, and every mapped suite is marked target-backed. `tests/migrationFoundation.test.mjs` checks the count. | High for inventory completeness and direct target mapping. |
| Kotlin/Spring scaffold | Source, Gradle wrapper, live `/health` smoke, structured errors, and Testcontainers tests exist. | High for scaffold and current health gate. |
| PostgreSQL ledger slice | Docker/JDK Testcontainers evidence is documented for ledger persistence, idempotency, outbox rows, approval-gated adjustments, loan disbursement/repayment/prepayment postings, card capture/reversal postings, and isolation behavior. The direct `tests/ledger.test.mjs` oracle is target-backed by Kotlin domain tests for double-entry/projection/reversal/unbalanced rejection plus PostgreSQL service tests for idempotency replay. The direct `tests/ledgerCore.test.mjs` oracle is now target-backed by PostgreSQL command-service tests for success paths, failure no-side-effect paths, duplicate reversal, closed-day immutability, concurrent no-overdraw isolation, posting-time `LIMIT_EXCEEDED` no-side-effect behavior, cumulative daily/monthly limit usage, reversal release, and concurrent no-over-limit behavior. `LoanDomainIntegrationTest` adds balanced `LOAN_DISBURSEMENT`, `LOAN_REPAYMENT`, and `LOAN_PREPAYMENT` evidence with projection equality and non-negative available balance. `CardDomainIntegrationTest` adds raw-PAN rejection, 3DS-required failure, card limit `LIMIT_EXCEEDED`, authorization hold/cancel, balanced `CARD_CAPTURE`, balanced capture `REVERSAL`, projection equality, non-negative available balance, and lost-card rejection evidence. | Medium-high for ledger slice, subject to retry policy and broader API parity. |
| Kotlin workflow state machines | Unit tests cover maker-checker, complaint answer approval, FDS release/block handoff, AML closure, and reconciliation adjustment handoff; durable PostgreSQL workflow repository tests now cover restart-safe state/events; maker-checker approval request/approve state is persisted through `PersistentApprovalService` and exposed through `/api/approvals`; complaint entry/list/answer/confirmation, customer-created FDS assign/release/block, AML, and reconciliation case execution routes have Spring integration tests. | Medium for durable state-machine parity; operational failure variants remain before retirement. |
| Temporal workflow contracts | `BankingCaseTemporalWorkflow` and `BankingCaseTemporalWorkflowIntegrationTest` prove Temporal worker/stub/query/signal execution for complaint answer, FDS release/block, AML closure, reconciliation adjustment, account hold, and account release. `LiveTemporalWorkerSmokeIntegrationTest` proves the Docker Compose Temporal server plus Spring Boot worker path, an SDK worker restart drill, actual Compose `core-banking-temporal-worker` container kill/restart drills for all current case types, Compose `temporal` server container restart drills for all current case types, Compose `postgres` crash/restart drills for all current case types, a Compose platform host-crash-shaped restart drill where PostgreSQL, Temporal, and the worker are killed together for all current case types, live signal/completion execution for all current case types, and live rejection/self-approval failure transitions; `TemporalWorkflowReferencePersistenceIntegrationTest` proves workflow ID/run ID persistence in domain tables, `TemporalWorkerMetricsTest` proves lifecycle metrics, `ObservabilityActuatorIntegrationTest` proves `/actuator/prometheus` exposes those metrics, `TemporalWorkflowTraceLogIntegrationTest` proves workflow signal/completion logs carry trace/span IDs for all current case types plus AML rejection and FDS self-approval failure paths, `docs/test-evidence/temporal-workflow-trace-log-correlation.md` proves all current case-type signal/completion spans plus live rejection/failure transition spans are exported to Tempo, and the workflow integration suite includes a synthetic transient retry drill. | Medium-high for Temporal execution, persistence, SDK worker restart, all-current-case Compose container worker restart, all-current-case Temporal server container restart, all-current-case PostgreSQL restart, worker metrics, synthetic retry, all-current-case signal/completion workflow trace/log correlation, live rejection/self-approval failure trace/log correlation, and all-current-case platform host-crash-shaped restart. |
| Spring authorization and JWKS | Bearer token filter enforces route roles, customer ownership, manager-only approval routes, actor binding, structured auth errors, denial audit events, and CORS-readable denial responses for browser channel clients. `SecurityAuthorizationIntegrationTest` keeps simulator fallback coverage and asserts auth-denial CORS headers, `JwksAuthorizationIntegrationTest` proves signed RS256 JWT validation against a JWKS endpoint with simulator fallback disabled, `LiveKeycloakRealmIntegrationTest` proves live Keycloak realm import, staff/customer token propagation, customer ownership, denial audit, TOTP required-action enforcement, WebAuthn required-action enforcement, and segregated passkey recovery admin token roles, `KeycloakRealmPolicyTest` locks the synthetic realm WebAuthn policy and `PASSKEY_RECOVERY_ADMIN` role segregation, customer-web Playwright proves browser authorization-code token propagation for all current API-backed customer paths, staff-terminal Playwright proves branch-maker/manager-checker token propagation for masked lookup, privileged unmask, customer-change approval, and WebAuthn required action with a Chromium virtual authenticator, complaint-portal Playwright proves complaint handler/checker token propagation for answer approval plus workflow failure-state rendering, ops-console Playwright proves ops operator/checker token propagation for reconciliation adjustment plus workflow failure-state rendering, audit-console Playwright proves auditor token propagation for hash-chain read-model evidence, and FDS/AML-console Playwright proves risk reviewer/checker token propagation for risk read-model, FDS release/block approval, AML closure approval, and workflow failure-state rendering. | Medium-high for RBAC/ABAC, JWKS, live realm import, local MFA/WebAuthn required-action evidence, synthetic WebAuthn policy/recovery segregation, current customer-web login propagation, staff-terminal login propagation, privileged unmask, WebAuthn smoke, complaint-portal login propagation, ops-console login propagation, audit-console login propagation, FDS/AML-console login propagation, and the generated non-synthetic passkey operations artifact. |
| Kafka/Outbox delivery | `KafkaOutboxPublisher` publishes persisted outbox rows to Redpanda after durable insert, marks rows `PUBLISHED` only after broker ack, records failed/DLQ state on publish failure, and `KafkaInboxConsumer` records idempotent inbox rows. `RedpandaOutboxDeliveryIntegrationTest` proves actual Redpanda publish, replay consume duplicate handling, retry, DLQ, and crash-before-mark-published replay with inbox idempotency. `OutboxWorkerRunnerTest` proves the scheduled/manual worker entrypoint uses configured publisher settings including the synthetic post-ack crash switch, `OutboxWorkerMetricsTest` proves worker lifecycle and delivery counters, `ObservabilityActuatorIntegrationTest` proves actuator Prometheus exposure, `docker compose --profile platform config` proves the platform worker and scrape target are configured, `LiveOutboxWorkerSmokeIntegrationTest` proves deployed restart-before-publish, post-broker-ack crash/replay, and Compose platform host-crash-shaped recovery paths against PostgreSQL and Redpanda, `LiveCustomerTransferApiCrashIntegrationTest` proves an API process halt after a durable customer-transfer commit leaves one ledger transaction, one customer-transfer result, and one `PENDING` outbox event, then replays the same idempotency key without duplicate side effects after restart, and `OutboxWorkerTraceLogIntegrationTest` plus a live Compose outbox trace drill prove worker batch logs carry trace/span IDs and outbox event IDs. | Medium-high for event backbone slice. |
| Next manifest channel shells | Customer-web plus staff, complaint, ops, audit, FDS/AML, and admin Next shells typecheck and have manifest-driven shell coverage. `npm run test:screen-engine` now proves the target TypeScript screen-engine manifest contract for channel coverage, admin privileged parameter maker-checker metadata, high-risk staff maker-checker metadata, staff PII masking/reason policy, and source-of-truth/control SQL tables. All seven channel apps have API-backed read-model smoke tests against the live Spring API with synthetic simulator tokens or a live Keycloak-backed browser path; admin-console has Spring integration-tested platform summary API coverage and live `security-admin01` Keycloak browser propagation evidence. Customer-web also has a live Keycloak Authorization Code + PKCE smoke for all current API-backed customer paths; customer-web has Spring-backed masked account detail with customer self-service `ACCOUNT_VIEW` audit, browser-backed transfer retry, insufficient-balance failure, ledger-backed transaction history, held FDS transfer status, durable held/failed transfer status, complaint entry, complaint confirmation, and API-backed loan/card smoke panel wiring; staff-terminal has a browser-backed privileged unmask smoke with branch-role denial visibility, a customer information change plus maker-checker approval smoke that also asserts self-approval rejection, and a live Keycloak Authorization Code + PKCE smoke for masked lookup, privileged unmask, plus branch-maker/manager-checker customer-change approval; complaint-portal has a browser-backed answer draft plus maker-checker approval smoke, a duplicate answer-draft failure-state smoke for `CMP-SYN-FAIL-001`, and a live Keycloak Authorization Code + PKCE smoke for those same command/failure paths; FDS/AML console has browser-backed FDS release, FDS block, and AML closure maker-checker approval smokes, including a posted ledger transaction for release, no ledger posting for block, STR simulation evidence for closure, duplicate release failure-state smoke for `FDS-SYN-FAIL-001`, duplicate closure failure-state smoke for `AML-SYN-FAIL-001`, and a live Keycloak Authorization Code + PKCE smoke for those same risk command/failure paths; ops-console has a browser-backed reconciliation adjustment maker-checker approval smoke with an adjustment ledger transaction, a duplicate adjusted-item failure-state smoke for `REC-SYN-FAIL-001`, and a live Keycloak Authorization Code + PKCE smoke for those same reconciliation command/failure paths; audit-console has a live Keycloak Authorization Code + PKCE smoke for hash-chain read-model evidence. | Medium-high for target manifest contract parity, shell parity, read-model smoke, current customer-web login propagation and self-service account-view audit, staff-terminal login propagation, privileged unmask, complaint-portal login propagation, ops-console login propagation, audit-console login propagation, FDS/AML-console login propagation, admin target surface structure, Spring admin API integration, and admin live browser smoke, customer transfer history/held-status/held-failed-status, customer complaint entry/list/confirmation target parity, command/failure smokes, and four workflow failure-state smokes; operational failure drills and final review remain before retirement. |
| Node retirement guard | `docs/migration/node-retirement-gate.json` is blocked, now includes an explicit `non-synthetic-passkey-operations` pending gate, and the retirement-gate command is documented as passing with blocked status. The Node service-shaped oracle modules now live under `legacy-node-reference/services` instead of target `services/`, and the legacy static app/UI shells now live under `legacy-node-reference/apps` and `legacy-node-reference/ui/public` instead of target `apps/*/public` or `packages/ui/public`. `tests/migrationFoundation.test.mjs` asserts target service directories contain no `.mjs` business modules, and `tests/nextScaffold.test.mjs` asserts target Next app directories contain no legacy static shells. `npm run retirement:audit` now checks that `.mjs` files remain inside approved reference/support paths, target app/package directories do not contain legacy static shells, source files do not import legacy Node modules through target `services/`, current target-stack anchors exist, listed gate evidence paths exist, and the parity map remains 42/42 target-backed. `docs/test-evidence/passkey-non-synthetic-operations.md` defines the manual evidence boundary, `scripts/record-passkey-non-synthetic-evidence.ts` creates the redacted JSON artifact only after explicit non-virtual passkey confirmations and masked staff panel assertions, and `npm run node:retirement-gate` rejects a pass claim unless the required non-virtual passkey artifact exists. | High. |

## Evidence Gaps

| Gap | Why it matters | Required evidence |
| --- | --- | --- |
| Live Spring Boot smoke | Completed through Docker/JDK against isolated PostgreSQL on port 15432; `/health` returned `status=ok`, `syntheticOnly=true`, and the platform worker container applied Flyway through v010. | Keep repeatable logs in future evidence pack output. |
| Host target workflow | Current docs say host Java/Gradle are unavailable, so target commands only pass through Docker/JDK. | Either install/document JDK 21 workflow or add a committed application container path for repeatable target runs. |
| Full API parity | Direct Node oracle suites now have target-backed Spring/Next coverage for the 42 mapped scenarios, including health, reason-required staff search, customer transfer idempotency, withdrawal/reversal API invariants, customer, complaint, FDS/AML, and reconciliation route semantics. | Broaden operational failure variants and complete final retirement review before deleting Node. |
| Durable audit/masking/approval | PostgreSQL-backed maker-checker request/approve state, approval API structured errors, reason-required staff access before audit append, append-only audit hash-chain continuity, default customer/account masking, staff detail/account/transaction reads, privileged unmasking, customer info change execution, complaint answer send execution, FDS release/block execution, AML closure execution, reconciliation adjustment execution, masking logs, and audit hash-chain append are now tested. The direct maker-checker oracle now has unit, PostgreSQL service, and Spring API no-side-effect assertions for missing reason and self-approval denials; staff terminal target parity also asserts missing-reason, unauthorized-role, and self-approval denials leave audit/approval state unmutated before valid checker execution. | Broader denial audit events and role-policy enforcement still need stronger target tests. |
| Workflow operations | Durable generic workflow repository tests, complaint answer API state persistence, customer-created FDS assign/release/block API state persistence, AML closure API state persistence, reconciliation adjustment API state persistence, Temporal test-environment workflow contracts, live Temporal server/worker smoke, workflow ID persistence in domain tables, worker lifecycle metrics, `/actuator/prometheus` metric exposure, a synthetic Temporal retry drill, a live SDK worker restart drill, live Compose worker container restart drills for all current case types, live Compose Temporal server container restart drills for all current case types, live Compose PostgreSQL restart drills for all current case types, live Compose platform host-crash-shaped restart for all current case types, and API-backed complaint/FDS/AML/reconciliation workflow failure-state browser smokes are now present. | Keep extending workflow orchestration and operator exception paths as new workflow states are introduced. |
| Kafka/Outbox delivery operations | Actual Redpanda publish, duplicate replay idempotency, retry, DLQ, and crash-before-mark-published replay are proven through Testcontainers. The target-stack outbox worker entrypoint, Compose service, Prometheus scrape target configuration, Micrometer/actuator metrics, live Compose worker-container restart-before-publish path, live Compose post-broker-ack crash/replay path, live Compose API post-commit crash/replay path, live Compose platform host-crash-shaped recovery path, and outbox worker trace/log correlation are now proven. | Keep extending eventing drills as new consumers and projections are introduced. |
| Next.js channel parity | Six channel shells have Playwright manifest parity. All six channel apps exercise Spring API-backed read-model calls when `BANKING_LAB_E2E_API_BASE_URL` is set. Customer-web additionally proves live Keycloak Authorization Code + PKCE token propagation for all current API-backed customer paths when `BANKING_LAB_E2E_KEYCLOAK_BASE_URL` is set. Staff-terminal additionally proves live Keycloak branch/checker token propagation for masked lookup, privileged unmask, and customer-change approval. Complaint-portal additionally proves live Keycloak complaint handler/checker token propagation for answer approval and duplicate answer workflow failure-state. Ops-console additionally proves live Keycloak ops operator/checker token propagation for reconciliation adjustment and duplicate adjusted-item workflow failure-state. Audit-console additionally proves live Keycloak auditor token propagation for hash-chain read-model evidence. FDS/AML-console additionally proves live Keycloak risk reviewer/checker token propagation for FDS release, FDS block, AML closure, and duplicate workflow failure-state. Customer-web executes Spring customer transfer retry, insufficient-balance structured error, customer transaction history, held FDS status, durable held/failed transfer status, complaint entry, and complaint confirmation smokes; staff-terminal executes Spring privileged unmask with branch-role denial visibility, plus a customer information change request and approval command smoke for `SYN-CUS-CMD-001` that asserts `MAKER_CHECKER_SELF_APPROVAL_REJECTED`; complaint-portal executes a Spring answer-draft plus approval command smoke for `CMP-SYN-CMD-001` and a duplicate answer-draft structured error smoke for `CMP-SYN-FAIL-001`; FDS/AML console executes FDS release, FDS block, AML closure, duplicate released-case structured error, and duplicate closed-case structured error smokes for `FDS-SYN-CMD-001`, `FDS-SYN-BLOCK-CMD-001`, `AML-SYN-CMD-001`, `FDS-SYN-FAIL-001`, and `AML-SYN-FAIL-001`; ops-console executes a reconciliation adjustment request plus approval command smoke for `REC-SYN-CMD-001` and a duplicate adjusted-item structured error smoke for `REC-SYN-FAIL-001`. | Gate closed for current API-backed channel parity; keep extending operator exception paths as new workflow states are introduced. |
| Keycloak authorization operations | Signed JWT/JWKS validation is proven locally with simulator fallback disabled. Live Keycloak realm import, public channel token issuance, audience/customer-claim mapping, Spring JWKS validation, TOTP required-action blocking, WebAuthn required-action direct-grant blocking, explicit synthetic WebAuthn policy, passkey recovery admin role segregation, staff-terminal WebAuthn browser completion with a virtual authenticator, customer-web browser token propagation for all current API-backed customer paths, staff-terminal branch/checker browser token propagation for masked lookup, privileged unmask, and customer-change approval, complaint-portal handler/checker browser token propagation, ops-console operator/checker browser token propagation, audit-console auditor browser token propagation, FDS/AML-console risk reviewer/checker browser token propagation, and non-synthetic passkey operations through `docs/test-evidence/generated/passkey-non-synthetic-evidence.json` are proven. | Keep enterprise recovery runbook and production deployment evidence separate from this synthetic lab retirement claim. |
| Security verification | `npm run security:evidence` records repeatable SCA/SAST/container/SBOM/DAST evidence. Current generated evidence has `npm audit --audit-level=high`, Semgrep SAST, Trivy filesystem scan, and CycloneDX SBOM generation passing through local tools or Docker scanner fallbacks. DAST is skipped in `docs/test-evidence/generated/security-evidence-summary.json` because `BANKING_LAB_DAST_URL` is not set. `npm run security:evidence:docker` now forces Docker scanner paths when Docker is available. | Rerun `npm run security:evidence:docker` before final security refresh, and run DAST only with a live synthetic lab URL supplied through `BANKING_LAB_DAST_URL`. |
| Observability | Spring exposes Temporal worker and outbox worker metrics through Micrometer and `/actuator/prometheus`, Prometheus config scrapes `core-banking`, `core-banking-temporal-worker`, and the configured `core-banking-outbox-worker`; `docs/test-evidence/observability-stack-smoke.md` records live readiness for Prometheus/Grafana/Loki/Tempo, Prometheus target/query evidence for core-banking, the Temporal worker, and Redpanda, Loki ingestion of `observability.workflow event=failed`, a firing `TemporalWorkflowFailed` ruler alert, and Grafana dashboard provisioning. `docs/test-evidence/opentelemetry-trace-log-correlation.md` records a live Spring `/health` trace exported to Tempo with the same trace ID in the Spring access log, `docs/test-evidence/temporal-workflow-trace-log-correlation.md` records all-current-case Temporal workflow signal/completion logs plus live rejection/failure transition logs with Tempo traces, and `docs/test-evidence/outbox-trace-log-correlation.md` records outbox worker batch trace/log correlation. | Keep extending alert routing evidence before production-like claims. |

## Missing Features Goal Delta

Review date: 2026-06-04

The Node retirement gate remains ready for the previous migration objective. The broader portfolio-completion objective in `docs/codex/implementation_missing_features_goals.md` is now covered by the phased evidence tracked in `docs/implementation-coverage-matrix.md`; future hardening items remain documented where they are not claimed.

| New gap | Current state | Required evidence |
| --- | --- | --- |
| Staff command depth | Customer profile change, privileged unmask, FDS/AML decisions, complaint answer approval, reconciliation adjustment, account hold/release, transfer-limit change, KYC review, fee waiver, transaction correction, deposit product rate-change requests, and fee policy parameter-change requests are API-backed in source. The account hold/release slice adds `account_hold_requests`, Spring routes, api-client methods, staff-terminal conditional smoke, and a StaffAccess integration test. The transfer-limit slice adds `account_limit_change_requests`, reason-required limit lookup, Spring routes, api-client methods, staff-terminal conditional smoke, and a StaffAccess integration test. The KYC review slice adds `customer_kyc_review_requests`, Spring routes, api-client methods, staff-terminal conditional smoke, and StaffAccess integration coverage that asserts no real KYC provider call and no ledger source-row mutation. The fee waiver slice adds `fee_waiver_requests`, Spring request/approve/reject routes, api-client methods, staff-terminal conditional smoke, operational fee waiver state, and targeted posted-fee refund through approved reversal when a single-account `FEE_POSTING` transaction is referenced. The transaction correction slice adds `transaction_correction_requests`, Spring request/approve/reject routes, api-client method, `LED-103` staff-terminal conditional smoke, and StaffAccess integration coverage that asserts maker-checker, closed-day rejection, append-only original ledger rows, and a balanced approved `REVERSAL` posting. The deposit product slice adds `deposit_rate_change_requests`, product/rate APIs, api-client methods, `PRD-102` conditional staff-terminal smoke, and Product integration coverage that asserts maker-checker, self-approval rejection, and approval-gated rate versioning. The fee policy slice adds `fee_policy_change_requests`, policy/version APIs, api-client methods, `FEE-103` conditional staff-terminal smoke, and Product integration coverage that asserts maker-checker, self-approval rejection, and approval-gated fee amount versioning. | Broaden API-backed staff command coverage only as later Phase D/F/G workflows introduce new operator actions. |
| Deposit product, fee, and interest module | Deposit product catalog, account enrollment, rate versioning, interest accrual, and interest posting batch state now exist through `V018__deposit_products_interest.sql`. `DepositProductApiIntegrationTest` proves accrual generation, approval-gated rate versioning, balanced `INTEREST_POSTING` ledger postings, idempotent batch replay without duplicate postings, and closed business date rejection. Fee policy catalog, fee policy versioning, fee posting batches, and fee refund reversal state now exist through `V019__fee_policies_posting.sql`. `FeePolicyApiIntegrationTest` proves reason-required fee inquiry audit, approval-gated fee amount versioning, balanced `FEE_POSTING` ledger postings, idempotent batch replay, targeted refund by approved `FEE_WAIVER` reversal, and closed business date rejection. | Keep Phase C evidence current when additional product/fee policy variants are introduced. |
| Posting-time limit enforcement | `V020__limit_usage_counters.sql` adds channel-aware daily/monthly usage counters and `LedgerCommandService` now enforces per-transaction, daily, monthly, and per-channel limits before withdrawal/internal-transfer posting. `LedgerCommandServiceIntegrationTest` proves over-limit no-side-effect behavior, cumulative daily/monthly counters, counter reset, concurrent burst limit preservation, and reversal release. `npm run formal:ledger` now includes `LimitUsageWithinConfigured` and `LimitUsageMatchesPostedDebits`. | Keep extending the same counter path when card authorizations/captures and loan repayment commands are introduced. |
| EOD closing pipeline | `V021__eod_closing_steps.sql` adds durable EOD step state, `POST /api/ops/eod/close` creates an `EOD_CLOSING` maker-checker request, `GET /api/ops/eod/{businessDate}` fills the `OPS-101` monitor gap, and approved execution runs interest accrual, interest posting, fee posting, reconciliation, and daily closing. `EodClosingPipelineIntegrationTest` proves reason-required request, self-approval rejection, checker execution, idempotent closed-date replay, durable step outbox/audit rows, closed-date posting rejection, balanced postings, projection equality, and simulated resume after a committed first step. | Add live Temporal EOD activity/container restart evidence after the EOD workflow is fully delegated to Temporal activities. |
| Statement and certificate read models | `StatementService` now exposes `GET /api/customers/{customerId}/statements`, `GET /api/transactions/{transactionId}/confirmation`, `GET /api/accounts/{accountId}/balance-certificate`, and `GET /api/customers/{customerId}/access-history`. `StatementReadModelIntegrationTest` proves statement debit/credit totals, opening/closing balance from postings, deterministic transaction confirmation and balance certificate IDs, customer ownership denial, staff reason-required denial, customer-scoped access history, and no ledger writes from statement APIs. | Add explicit historical balance snapshot tables if future certificates must reproduce historical available balance/hold state rather than signed posting balance. |
| Loan domain | `V022__loan_domain.sql` adds loan product/application/loan/schedule/accrual/payment state, `LoanService` adds deterministic synthetic underwriting and approval-backed execution, `LedgerCommandService` posts balanced loan disbursement/repayment/prepayment transactions, and `LoanDomainIntegrationTest` proves maker-checker self-approval rejection, schedule principal/interest totals, overdue accrual replay, full prepayment idempotency, outbox events, projection equality, and non-negative available balance. | Add partial prepayment schedule-recast variants, Temporal-backed long-running review states, and browser/live-Keycloak evidence when Phase 2 is broadened. |
| Card domain | `V023__card_domain.sql` adds card/card-limit/3DS/authorization/capture state, `CardService` stores tokenized PAN values only, `LedgerCommandService` posts balanced card capture and reversal transactions, and `CardDomainIntegrationTest` proves raw-PAN rejection, high-value 3DS enforcement, authorization hold/cancel, limit `LIMIT_EXCEEDED`, capture posting, reversal, lost-card rejection, outbox events, projection equality, and non-negative available balance. | Add partial captures, merchant/category controls, dispute/replacement workflows, and browser/live-Keycloak evidence when Phase 3 is broadened. |
| Parameter-admin APIs | `V024__parameter_admin_controls.sql` adds effective-dated parameter/version pairs for reconciliation, audit retention, FDS rules, security policy, and menu-role controls, plus `parameter_change_requests`. `ParameterAdminService` exposes current/scheduled/history/change APIs, `StaffAccessService` applies/rejects the new high-risk approvals, and `CustomerTransferService` reads `fds.highAmountMinor` for detection. `ParameterAdminIntegrationTest` proves approval-before-effect, future scheduling, self-approval rejection, rollback-as-new-version, unauthorized actor rejection, and FDS threshold behavior change. | Add browser smoke buttons per ops/audit/FDS/admin app and richer JSON validation for menu-role parameters before claiming live-channel parameter operation. |
| Python/DuckDB FDS/AML analytics | `analytics/aml-fds-python` now has a DuckDB in-memory mart, feature store, deterministic rules/anomaly scoring, synthetic sample transactions, JSON/CSV export, `npm run analytics:fds-aml:test` unit coverage, and generated JSON/CSV artifacts. `FdsAnalyticsEvidenceService` exposes `GET /api/fds/analytics`, validates synthetic controls, requires a business reason, appends `FDS_ANALYTICS_EVIDENCE_VIEW`, and the FDS/AML console reads the evidence through `createBankingApiClient().fdsAnalyticsEvidence(...)` instead of importing the JSON file. `FdsAnalyticsEvidenceIntegrationTest`, `next:fds-aml-console:typecheck`, `packages:typecheck`, and `tests/nextScaffold.test.mjs` passed locally. | Keep the API read path synthetic-only; add live browser/Keycloak evidence only when a running synthetic stack is available. |
| Formal verification | `npm run formal:ledger` now writes `docs/test-evidence/generated/formal-ledger-tlc-result.json` with `staticOnly: false` and bounded state-search results for ledger, posting-time limit usage, and idempotency invariants. `tlc -help` failed with `command not found`, so actual TLC is still unavailable in this environment and the formal row remains partial rather than complete. | Add a pinned TLC binary/JAR or approved Docker TLA+ runner, then run actual TLC for `formal/Ledger.tla` and `formal/Idempotency.tla`. |
| Kubernetes/Helm validation | `npm run k8s:validate` and `npm run helm:template` now pass and generate structural evidence. `kubectl cluster-info` failed after sandbox escalation with localhost API connection refused, and Helm CLI is not installed, so structural validation is complete but live cluster deployment validation is missing. | Run against a disposable cluster with Helm installed and record server-side dry-run or install/uninstall evidence. |
| Load and backup/restore operations | `npm run load:synthetic` now executes the selected Phase G local synthetic mixed-banking traffic smoke through `tests/load/mixed-banking-traffic.mjs`, writes `docs/test-evidence/generated/load-test-summary.json` and `docs/test-evidence/load-test-summary.md`, and records customer account lookup, customer transfer, idempotency retry burst, staff lookup with audit append, FDS held-case lookup, approval inbox lookup, ledger invariant validity, and audit hash-chain validity. `npm run postgres:backup-drill` now writes fixture-mode evidence with ledger count parity, balanced postings, audit hash-chain, approval state, workflow state, and customer transfer result checks. `npm run postgres:backup-drill -- --mode=live` was attempted and failed because `BANKING_LAB_POSTGRES_URL` and `BANKING_LAB_RESTORE_POSTGRES_URL` are unset. | Live PostgreSQL restore proof still requires disposable source/restore PostgreSQL URLs; local `pg_dump`, `pg_restore`, and `psql` are installed. |
| Security evidence skipped causes | Current generated security evidence records npm audit, Semgrep, Trivy, and SBOM passing after sandbox escalation, and records `dast-zap-baseline` as skipped because `BANKING_LAB_DAST_URL` is not set. The Docker-forced rerun path is documented in `docs/test-evidence/security-docker-rerun.md`; if Docker or scanner images are unavailable in a future run, the generated summary must record those as explicit skipped causes instead of silently reusing old artifacts. | Run DAST only against a live synthetic lab URL by setting `BANKING_LAB_DAST_URL`; do not mark DAST passed without that live target. |
| Backend integration repeatability in this local run | `npm run test:core-banking:unit -- --rerun-tasks` passed. The targeted StaffAccess Testcontainers suite passed with `npm run test:core-banking:integration -- --tests lab.banking.core.staff.StaffAccessApiParityIntegrationTest`. The Phase C product-interest slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.product.DepositProductApiIntegrationTest`. The Phase C fee-policy slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.product.FeePolicyApiIntegrationTest --rerun-tasks`. The Phase 1B EOD slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.eod.EodClosingPipelineIntegrationTest` after sandbox escalation for Gradle file-lock socket access. The Phase 1C statement slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.statement.StatementReadModelIntegrationTest` after sandbox escalation. The Phase 2 loan slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.loan.LoanDomainIntegrationTest` after sandbox escalation and a fixed test Flyway path. The Phase 3 card slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.card.CardDomainIntegrationTest` after sandbox escalation and a corrected over-limit assertion path. The Phase 4 parameter-admin slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.parameters.ParameterAdminIntegrationTest` after sandbox escalation and fixed compile/fixture/reset issues. The Phase 5 analytics read API slice passed with `npm run test:core-banking:integration -- --tests lab.banking.core.analytics.FdsAnalyticsEvidenceIntegrationTest` after sandbox escalation. | Rerun the full integration suite or rely on CI for all Testcontainers classes before merging broad backend changes. |

## Evidence Quality Risks

- Generated evidence files under `docs/test-evidence/generated` are useful summaries, but they are not a substitute for command outputs from the target stack.
- The Node evidence pack can remain green while target parity is still incomplete.
- Structural tests can confirm files and route declarations, but they do not prove runtime behavior, transaction boundaries, restart survival, or authorization enforcement.
- Audit hash-chain appends now use a PostgreSQL lock row for concurrent API smoke paths, and staff approval execution, staff audited customer detail, customer transfer commands, FDS/AML decision request creation, plus reconciliation adjustment request creation have bounded SERIALIZABLE retry for transient command-approval conflicts. Broader bank-grade command paths still need explicit retry or operator-visible failure policies for other serialization conflicts.

## Recommended Next Evidence Slice

The next smallest safe evidence slices are live-environment hardening items that are not claimed by the current generated artifacts:

1. Run `npm run postgres:backup-drill -- --mode=live` against disposable synthetic PostgreSQL source/restore databases.
2. Run `BANKING_LAB_DAST_URL=<synthetic-lab-url> npm run security:evidence:docker` when a live local target and Docker scanner images are available.
3. Run Kubernetes/Helm validation against a disposable cluster and record server-side dry-run or install/uninstall evidence.

Node is retired only from target-path dependency for the current synthetic lab scope; the archived oracle/reference boundary remains available for regression comparison.
