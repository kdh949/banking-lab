# Implementation Coverage Matrix

Review date: 2026-06-10

Scope: target-stack Banking Lab implementation only. The synthetic runtime helper is retained for bounded scenario evidence and is not counted as target-path implementation coverage.

Status values are limited to `complete`, `api-backed-read`, `api-backed-command`, `browser-e2e-backed`, `live-keycloak-backed`, `route-backed-live-gated`, `backend-control-covered`, `manifest-only`, `partial`, `missing`, and `not-applicable`.

Status meanings:

- `complete`: implemented and verified for the stated synthetic lab scope.
- `api-backed-read`: read path calls a target Spring or bounded-context API.
- `api-backed-command`: command path calls a target API and preserves the stated controls.
- `browser-e2e-backed`: browser route exercised the target behavior end to end.
- `live-keycloak-backed`: browser/API path was verified with live Keycloak/JWKS propagation.
- `route-backed-live-gated`: route exists and live execution is gated by environment variables or a running stack.
- `backend-control-covered`: backend controls are implemented and tested, but the current iWorks staff frontend does not expose a dedicated route.
- `manifest-only`: structural manifest coverage only.
- `partial`: meaningful target implementation exists, but the row still has unclosed evidence or feature depth gaps.
- `missing`: no target implementation is present.
- `not-applicable`: the control or layer does not apply to the row.

## Phase 0 Remaining Hardening Baseline

The 2026-06-06 remaining-hardening baseline is recorded in `docs/codex/remaining-hardening-status.md`. It confirms the existing target-stack coverage below should not be duplicated, and it scopes the next hardening work to CI full-service verification, Spring Security Resource Server standardization, ledger projection drift/rebuild operations, route-level customer/staff workflows, contract validation scripts, secrets/observability/runbooks, bounded-context CI/contract gates, and large-ledger operational evidence.

Current Phase 0 command evidence reran `npm ci`, `npm test`, `npm run validate:manifests`, `npm run packages:typecheck`, `npm run scripts:typecheck`, `docker compose config`, `npm run test:core-banking:unit`, and `npm run test:core-banking:integration`. The two Gradle commands failed inside the sandbox with a file-lock socket denial and passed after approved unsandboxed reruns.

## Phase 1 CI Coverage Hardening

The Phase 1 CI hardening evidence is recorded in `docs/test-evidence/ci-coverage-hardening.md`. The default PR workflow now includes explicit payment-service, notification-service, reporting-service, aggregate Gradle unit, platform structural, contract structural, and Docker Compose profile config jobs. Contract validation is intentionally structural in this phase; full OpenAPI/AsyncAPI drift prevention remains assigned to Phase 4.

## Phase 2 Spring Security Resource Server Hardening

The Phase 2 security hardening evidence is recorded in `docs/test-evidence/spring-security-resource-server-hardening.md`. Core-banking, payment-service, notification-service, and reporting-service now include Spring Security OAuth2 Resource Server dependencies and stateless filter-chain configuration. Signed JWT verification goes through Spring/Nimbus with issuer/audience validation, simulator tokens require dev/test double opt-in and fail in prod-like profiles, and selected high-risk core APIs have method-level authorization gates while preserving existing route policy, structured error, audit, step-up, trusted-device, and session-revocation behavior.

## Phase 5 Ledger Projection Integrity Workflow

The Phase 5 ledger projection evidence is recorded in `docs/test-evidence/ledger-projection-integrity-workflow.md`. Core banking now has durable projection drift runs, drift items, rebuild requests, rebuild runs, rebuild items, reason-required drift APIs, maker-checker rebuild approval, idempotent rebuild execution, before/after source and projection hashes, ops-console manifests, shared API-client methods, and targeted `LedgerProjection*` Testcontainers coverage.

## Phase 3 Customer And Staff Route Workflow Hardening

The Phase 3 route evidence is recorded in `docs/test-evidence/phase-3-workflow-route-hardening.md`. Customer-web now exposes route-backed workflow pages for login, accounts, account detail, transfers, transfer result/status, complaints, complaint detail, cards, card detail, loans, payments, notifications, and security. Staff-terminal has been converted to the current iWorks integrated terminal shell and no longer uses staff manifests, API-backed route pages, or the retired staff manifest renderer; Spring staff-control APIs remain covered by backend and security tests.

## Phase 4 Contract Validation Hardening

The Phase 4 contract validation evidence is recorded in `docs/test-evidence/contract-validation-hardening.md`. Core-banking and reporting-service now have checked-in OpenAPI contracts, payment and notification OpenAPI contracts declare structured-error metadata, the AsyncAPI backbone declares banking event-envelope fields, reporting domain events have JSON Schemas, and root `contracts:lint`, `contracts:check-client`, and `contracts:check-events` gates are wired into CI.

| Area | Feature/Screen | Manifest | Next UI | API Client | Spring API | PostgreSQL | Keycloak/AuthZ | Audit | Maker-checker | E2E/Integration Test | Evidence | Status |
| ---- | -------------- | -------- | ------- | ---------- | ---------- | ---------- | -------------- | ----- | ------------- | -------------------- | -------- | ------ |
| customer-web | Synthetic signup/login: `CWB-001`, `CWB-002` | yes | yes; form-backed `/signup` and `/login` store synthetic session state from API responses | yes | yes; `POST /api/auth/customer/signup`, `POST /api/auth/customer/login`, authenticated `/api/auth/session` remains session proof | `customers`, `customer_kyc_profiles`, `customer_auth_identities` with password hashes and signup idempotency metadata | unauthenticated signup/login only; simulator token issuance requires dev/test opt-in and prod-like profiles fail fast | signup/login success/failure audit without plaintext password payloads | not-applicable | Spring auth integration, customer-web form route Playwright smoke, env-gated signup->login smoke | `docs/test-evidence/customer-onboarding-self-service.md`, `docs/codex/customer-onboarding-self-service-status.md` | api-backed-command |
| customer-web | Account overview/detail/history: `CWB-101`, `CWB-102`, `CWB-103` | yes | yes; form-backed `/accounts` and `/accounts/[accountId]` load owned accounts/details/history from session state | yes | yes; owned account list, account detail, transaction history, statement/confirmation/certificate reads | yes; ledger postings drive account projections, transaction history, statement artifacts, and certificate snapshots | customer ownership policy and customer token path when configured | self-service account list/detail/history audit with masked account numbers | not-applicable | Playwright conditional API smoke, Spring integration, statement read-model integration with persisted certificate snapshots, live customer Keycloak propagation for current API-backed paths, form route Playwright coverage | `docs/test-evidence/customer-onboarding-self-service.md`, `docs/test-evidence/api-backed-channel-smoke.md`, `docs/test-evidence/keycloak-live-realm-smoke.md`, `docs/test-evidence/statement-read-models.md` | api-backed-read |
| customer-web | Customer profile, onboarding intake, Customer 360, and statements: `CWB-003`, `CWB-004`, `CWB-104`, `CWB-105`, `CWB-106`, `CWB-107` | yes | yes; `/profile`, `/onboarding`, `/360`, `/accounts/[accountId]/statement`, and `/statements` use the shared self-service shell and API client | yes | yes; current-customer profile, idempotent self-service account-opening intake, Customer 360, account statement, consolidated statement, and artifact history endpoints | `customer_onboarding_checks`, `customer_self_service_account_opening_requests`, `statement_artifact_snapshots`, account/ledger/audit source rows | current `CUSTOMER` token ownership enforced; staff path remains separate for account execution | profile, account-opening request/status, 360, statement, and artifact-history audit events with self-service masking | account execution remains staff maker-checker; customer intake itself creates no account or ledger posting | static Node tests, contract/client/OpenAPI gates, customer-web build/typecheck, Playwright static route smoke, and checked-in `CustomerSelfService360IntegrationTest`; runtime Kotlin execution is pending under supported JDK | `docs/test-evidence/customer-self-service-360.md` | partial |
| customer-web | Internal transfer and transfer result/status: `CWB-201`, `CWB-202`, `CWB-203` | yes | yes; form-backed `/transfers/new` and `/transfers/[resultId]` use owned source accounts, internal recipient lookup, idempotency, and result state | yes | yes; internal recipient lookup, transfer command, transfer status/history | ledger postings and transfer result read model; system accounts are rejected as customer transfer recipients | customer ownership policy and active internal synthetic recipient policy | command audit, recipient lookup audit, and outbox | not-applicable | synthetic runtime, Spring integration, browser command/status smoke, env-gated signup->login->accounts->transfer->history smoke, live customer Keycloak propagation | `docs/test-evidence/customer-onboarding-self-service.md`, `docs/test-evidence/phase-4-customer-web.md`, `docs/test-evidence/keycloak-live-realm-smoke.md` | api-backed-command |
| customer-web | Complaint entry/list/answer confirmation: `CWB-301`, `CWB-302`, `CWB-303` | yes | yes; route-backed `/complaints` and `/complaints/[caseId]` state pages | yes | yes | complaint tables | customer ownership policy | timeline and command audit | answer approval happens on staff path | synthetic runtime, Spring integration, browser complaint smoke, live customer Keycloak propagation, route-state Playwright coverage for waiting-approval/closed/ownership-denied states | `docs/test-evidence/phase-5-complaint-workflow.md`, `docs/test-evidence/keycloak-live-realm-smoke.md`, `docs/test-evidence/phase-3-workflow-route-hardening.md` | route-backed-live-gated |
| customer-web | Security and access history: `CWB-401` | yes | yes | yes | yes; `GET /api/customers/{customerId}/access-history` | audit read model filtered by customer | customer ownership policy; staff reason-required read allowed | access-history view audit | not-applicable | `StatementReadModelIntegrationTest` passed locally; customer-web typecheck covers panel wiring | `docs/test-evidence/statement-read-models.md` | api-backed-read |
| customer-web | Loan application/detail/repayment/prepayment: `CWB-501`, `CWB-502`, `CWB-503`, `CWB-504` | yes | yes; API-backed smoke panel hook | yes | yes | `loan_products`, `loan_applications`, `loans`, `loan_repayment_schedule`, `loan_payments`, balanced loan ledger postings | customer ownership policy; staff read/write reason checks | application/detail/repayment/prepayment audit | execution requires `LOAN_EXECUTION` maker-checker approval | `LoanDomainIntegrationTest` passed locally; customer-web typecheck covers panel wiring | `docs/test-evidence/loan-domain.md` | api-backed-command |
| customer-web | Card issue/authorization/capture/cancel/3DS/loss: `CWB-601`, `CWB-602`, `CWB-603`, `CWB-604`, `CWB-605`, `CWB-606` | yes | yes; API-backed smoke panel hook | yes | yes | `cards`, `card_limits`, `card_limit_usage_counters`, `card_3ds_simulations`, `card_authorizations`, `card_captures`, balanced card capture ledger postings | customer ownership policy plus route role policy | issue/3DS/authorization/capture/cancel/reversal/loss audit | not-applicable for customer self-service card lifecycle in this slice | `CardDomainIntegrationTest` passed locally; customer-web typecheck covers panel wiring | `docs/test-evidence/card-domain.md` | api-backed-command |
| customer-web | Notification preferences: `CWB-801` | yes | yes; API-backed smoke panel hook | yes; owned customer preference list/upsert methods | yes; `GET/PUT /api/notifications/customers/{customerId}/preferences` | notification recipient preferences and access audit table | CUSTOMER route policy plus token `customerId` ownership check | `NOTIFICATION_CUSTOMER_PREFERENCE_VIEW` self-service audit and preference actor/reason fields | not-applicable for customer self-service preference changes | `NotificationAuthorizationIntegrationTest`, customer-web typecheck, static Next scaffold coverage, and targeted Playwright parity passed locally | `docs/test-evidence/notification-service.md` | api-backed-command |
| customer-web | Notification delivery history: `CWB-802` | yes | yes; API-backed smoke panel hook | yes; owned customer delivery-history method | yes; `GET /api/notifications/customers/{customerId}/deliveries` | notification delivery requests and access audit table | CUSTOMER route policy plus token `customerId` ownership check | `NOTIFICATION_CUSTOMER_DELIVERY_HISTORY_VIEW` self-service audit with masked messages | not-applicable | `NotificationAuthorizationIntegrationTest`, customer-web typecheck, static Next scaffold coverage, and targeted Playwright parity passed locally | `docs/test-evidence/notification-service.md` | api-backed-read |
| staff-terminal | iWorks integrated terminal shell plus bounded Spring API evidence panel | no; staff manifests removed | yes; `IntegratedTerminalApp` with `/`, `/api/terminal-status`, and `StaffApiEvidencePanel` in the portal screen | yes; bounded panel calls `staffCustomerDetail` and `staffApprovals` only | `/api/terminal-status` returns client IP/server time; Spring staff detail and approval inbox are called when API env and simulator-token opt-in are configured | staff access/audit and approval tables through existing Spring APIs | simulator-token opt-in for local evidence; staff route role policy still enforced by Spring | status bar shows runtime connection metadata; staff detail lookup appends reason-required audit | not-applicable for read-only panel | `npm run integrated-terminal:boundary-check`, `npx playwright test apps/staff-terminal/e2e/integrated-terminal.spec.ts`, `npm run next:staff-terminal:typecheck`, `npm run next:staff-terminal:build`, `npm run test:staff-terminal:api-e2e-compose` | `docs/architecture/phase-3-staff-terminal.md`, `docs/test-evidence/phase-3-workflow-route-hardening.md`, `docs/test-evidence/live-route-api-execution.md` | route-backed-live-gated |
| staff-terminal | Spring staff access and high-risk staff controls | no; backend controls are not staff UI manifests | not exposed through the current iWorks frontend beyond navigation placeholders | shared API clients remain available for non-staff channel integrations and tests | yes; staff customer/account/transaction access, approval, unmask, account hold/release, transfer limit, KYC review, fee waiver, transaction correction, complaint/FDS/AML/reconciliation control APIs remain Spring-backed | staff access, approval, workflow, customer/account, limit, fee, FDS/AML, reconciliation, and ledger tables | staff/checker/manager/auditor/ops route policy plus reason-required service policy | reason-required audit, time-boxed unmask audit, request/approve/execute audit | yes for high-risk staff operations | Spring integration, security, approval, and passkey evidence tests; current staff frontend Playwright verifies only the iWorks terminal boundary | `docs/test-evidence/api-backed-channel-smoke.md`, `docs/test-evidence/keycloak-live-realm-smoke.md`, `docs/test-evidence/passkey-non-synthetic-operations.md` | backend-control-covered |
| call-center-console | Synthetic call-center workflow: `CALL-101`, `CALL-102`, `CALL-103`, `CALL-104`, `CALL-105`, `CALL-106` | yes | yes; dedicated `apps/call-center-console` renders reusable inquiry/command/case manifests and exposes API-backed simulator-token plus Keycloak-token smoke panels on port 3008 | yes; customer search, interaction start/detail, note entry, aftercall task, escalation request, approval execution, close, and history methods | yes; `GET/POST /api/staff/call-center/*` routes and shared `/api/staff/approvals/{approvalId}/approve|reject` execution routes are Spring-backed | `call_center_interactions`, `call_center_notes`, `call_center_aftercall_tasks`, `call_center_escalations`, `operator_approvals`, `call_center_access_audit`, and complaint source references created after escalation approval | `CALL_CENTER_AGENT`, `CALL_CENTER_MANAGER`, branch, complaint, compliance, and read-only auditor route roles; Keycloak realm declares the `call-center-console` client plus `call-agent01` and `call-manager01` synthetic users | reason-required call-center audit, masked search/history, redacted note storage, raw-note/raw-complaint audit-copy guards, maker-checker approval audit, self-approval rejection, and access-audit rows | yes; `CALL_CENTER_ESCALATION` approval requires independent checker before complaint conversion | `CallCenterWorkflowIntegrationTest` passed locally after sandbox escalation; manifest validation, package typecheck, Next build, OpenAPI/client contract gates, `KeycloakRealmPolicyTest`, Playwright manifest/token-route smoke source coverage, and `npm run test:call-center-console:keycloak-e2e-compose` passed after sandbox escalation with live Keycloak/JWKS agent/manager browser propagation and simulator tokens disabled | `docs/test-evidence/call-center-console.md`, `docs/test-evidence/generated/call-center-console.json` | partial |
| core-banking | Posting-time transfer limit enforcement | not-applicable | not-applicable | not-applicable | yes | `account_limits`, `limit_usage_counters`, ledger/idempotency/outbox rows | API route authorization inherited from ledger/customer paths | structured `LIMIT_EXCEEDED` and ledger command audit/outbox on successful posting | limit changes remain maker-checker through `LIM-102`; posting-time rejection is not-applicable | `LedgerCommandServiceIntegrationTest` passed locally for over-limit no-side-effect, cumulative daily/monthly counters, reversal release, and concurrent burst; `npm run formal:ledger` includes limit usage invariants | `docs/test-evidence/limit-enforcement.md`, `docs/test-evidence/generated/formal-ledger-tlc-result.json` | api-backed-command |
| core-banking | Synthetic customer onboarding: `CST-201`, `CST-202` | no staff-terminal manifest; customer self-service manifests remain under `customer-web` | not exposed through current iWorks staff frontend | yes for Spring staff/account-opening test harnesses | yes; staff maker-checker onboarding request/approval/execute APIs remain backend-covered | customer, KYC, approval, and audit tables | staff/checker separation with simulator-token fallback disabled in prod-like profiles | onboarding request/approve/execute audit without unmasked PII | yes | Spring staff access, customer self-service smoke, and evidence preflight tests | `docs/test-evidence/customer-onboarding-self-service.md`, `docs/test-evidence/api-backed-channel-smoke.md` | backend-control-covered |
| core-banking | Synthetic account opening: `ACC-201`, `ACC-202` | no staff-terminal manifest; customer self-service account-opening evidence remains | not exposed through current iWorks staff frontend | yes for request/approval/execute helper methods used by tests | yes; account opening request, independent approval, and execution APIs remain Spring-backed | account, ledger, approval, idempotency, and audit tables | staff/checker route policy plus separation-of-duties service policy | account-opening request/approve/execute audit with masked account identifiers | yes | Spring integration, customer self-service smoke, and evidence preflight tests | `docs/test-evidence/customer-onboarding-self-service.md`, `docs/test-evidence/api-backed-channel-smoke.md` | backend-control-covered |
| core-banking | Staff-controlled fee, KYC, transaction correction, product-rate, and loan operations: `FEE-101`, `FEE-102`, `FEE-103`, `KYC-101`, `LED-103`, `PRD-101`, `PRD-102`, `LON-101`, `LON-102`, `LON-103` | no staff-terminal manifest; backend control coverage only | not exposed through current iWorks staff frontend beyond navigation placeholders | shared API clients remain available for non-staff channel integrations and tests | yes; fee policy, KYC review, transaction correction, product parameter, and loan APIs remain Spring-backed | fee, KYC, correction, product, loan, ledger, approval, idempotency, and audit tables | staff/checker/manager/compliance route policy plus reason-required service policy | reason-required view/change audit and balanced reversal/posting audit where applicable | yes for high-risk operations | Spring integration, live Keycloak evidence, and passkey evidence tests; current staff frontend Playwright verifies only iWorks terminal boundary | `docs/test-evidence/api-backed-channel-smoke.md`, `docs/test-evidence/keycloak-live-realm-smoke.md`, `docs/test-evidence/passkey-non-synthetic-operations.md` | backend-control-covered |
| complaint-portal | Complaint intake/status/answer/closure: `CMP-101`, `CMP-102`, `CMP-104`, `CMP-105`, `CMP-108` | yes | yes | yes | yes | complaint tables | customer ownership policy | timeline/audit | staff approval before answer | synthetic runtime, Spring integration, browser command/failure smokes, live complaint Keycloak propagation | `docs/test-evidence/phase-5-complaint-workflow.md`, `docs/test-evidence/keycloak-live-realm-smoke.md` | live-keycloak-backed |
| complaint-portal | Additional materials, reopen, type guide: `CMP-103`, `CMP-106`, `CMP-107` | yes | yes; API-backed self-service smoke panel for type guide, material, and reopen | yes | yes; `GET /api/customer/complaint-types`, `POST /api/customer/complaints/{caseId}/materials`, and `POST /api/customer/complaints/{caseId}/reopen-requests` | `complaint_case_materials`, `complaint_reopen_requests`, complaint cases, and append-only complaint timeline | customer ownership policy through customer routes and route-level role policy | CMP-103 material audit masks free-form detail; CMP-106 reopen audit and timeline append | not-applicable for customer self-service material/reopen; staff answer approval remains maker-checker | `CustomerComplaintSelfServiceApiIntegrationTest`, complaint integration suite, complaint-portal typecheck, packages typecheck, static scaffold, and targeted Playwright manifest parity passed locally | `docs/test-evidence/phase-5-complaint-workflow.md` | api-backed-command |
| complaint-portal | Transfer/card dispute source-linked intake: `CMP-109`, `CMP-110` | yes | yes; API-backed self-service panel can create transfer and card disputes with synthetic source references | yes | yes; `POST /api/customer/complaints` accepts `sourceReference` for `TRANSFER_DISPUTE` and `CARD_DISPUTE` | `complaint_cases.source_reference_json` plus existing `customer_transfer_results`, `ledger_transactions`, `card_authorizations`, and `card_captures` source tables | customer-owned source validation rejects missing, unsupported, and cross-customer references | `COMMAND_REQUESTED` audit includes bounded synthetic source metadata without copying free-form complaint description | intake itself is self-service; downstream refund/reversal/adjustment execution remains maker-checker through existing correction paths | `CustomerComplaintEntryApiParityIntegrationTest`, complaint integration suite, complaint-portal typecheck, packages typecheck, static scaffold, manifest validation, and targeted Playwright manifest parity passed locally | `docs/test-evidence/phase-5-complaint-workflow.md` | api-backed-command |
| ops-console | Daily closing monitor: `OPS-101` | yes | yes | yes | yes; `POST /api/ops/eod/close` and `GET /api/ops/eod/{businessDate}` | `eod_closing_steps`, product/fee batch tables, reconciliation, daily closing, ledger/idempotency/outbox rows | ops route policy plus service role checks | request/approve/execute audit and per-step OPS-101 audit | yes for close request | `EodClosingPipelineIntegrationTest` passed locally for GET monitor, reason-required request, maker-checker close, idempotent replay, step resume, and closed-date rejection | `docs/test-evidence/eod-closing-pipeline.md`, `docs/failure-drills/eod-worker-restart-mid-pipeline.md` | api-backed-command |
| ops-console | Reconciliation item review and adjustment: `OPS-201`, `OPS-202` | yes | yes | yes | yes | reconciliation item taxonomy, adjustment request, and ledger posting | ops/checker roles | workflow audit | yes | synthetic runtime, Spring integration, browser command/failure smokes, live ops Keycloak propagation, and target duplicate/stale/external-only/missing-external simulator taxonomy coverage | `docs/test-evidence/fds-aml-reconciliation.md`, `docs/test-evidence/keycloak-live-realm-smoke.md` | live-keycloak-backed |
| ops-console | Interest accrual and posting batch: `OPS-401`, `OPS-402` | yes | manifest renderer | yes | yes | `interest_accruals`, `interest_posting_batches`, balanced `INTEREST_POSTING` ledger transaction | ops route and service policy | accrual run and posting execution audit | no for batch in first slice | Spring product integration test passed locally; no live browser API run locally | this matrix and `docs/test-evidence/api-backed-channel-smoke.md` | api-backed-command |
| ops-console | Fee posting batch: `OPS-403` | yes | manifest renderer | yes | yes | `fee_posting_batches`, balanced `FEE_POSTING` ledger transaction | ops route and service policy | posting execution audit | no for routine batch | `FeePolicyApiIntegrationTest` passed locally; no live browser API run locally | this matrix and `docs/test-evidence/api-backed-channel-smoke.md` | api-backed-command |
| ops-console | Reconciliation parameters: `OPS-301` | yes | manifest renderer with API-backed endpoints | yes | yes; `GET /api/ops/parameters/reconciliation`, history, and change request | `reconciliation_parameters`, `reconciliation_parameter_versions`, `parameter_change_requests` | ops route policy plus service role checks | parameter view/change/apply audit | yes | `ParameterAdminIntegrationTest` and `npm run test:parameter-command-e2e-compose` passed locally | `docs/test-evidence/parameter-admin-apis.md` | api-backed-command |
| ops-console | Ledger projection drift/rebuild: `OPS-LEDGER-101`, `OPS-LEDGER-102`, `OPS-LEDGER-103` | yes | API-backed smoke panel plus manifest renderer | yes; drift run, rebuild request, approve/reject, execute, and rebuild evidence methods | yes; `/api/ops/ledger/projection-drift-runs`, `/api/ops/ledger/projection-rebuild-requests`, and `/api/ops/ledger/projection-rebuild-runs/{runId}` | `ledger_projection_drift_runs`, `ledger_projection_drift_items`, `ledger_projection_rebuild_requests`, `ledger_projection_rebuild_runs`, `ledger_projection_rebuild_items`; rebuild mutates only `account_balance_projections` from `ledger_postings` | ops route policy, method-level role gates, and high-risk step-up policy for rebuild request/approve/reject/execute | drift run audit plus rebuild execution audit with source/projection hashes | yes for `LEDGER_PROJECTION_REBUILD`; self-approval rejected | `LedgerProjectionIntegrityIntegrationTest` and `LedgerProjectionRebuildWorkflowIntegrationTest` passed locally; ops-console Playwright live API smoke is wired and skipped without `BANKING_LAB_E2E_API_BASE_URL` | `docs/test-evidence/ledger-projection-integrity-workflow.md` | api-backed-command |
| audit-console | Hash-chain/access log: `AUD-101`, `AUD-102` | yes | yes | yes | yes for `/api/audit/events` | audit event table | auditor/compliance policy | hash-chain validation | not-applicable | Spring integration, browser read smoke, live auditor Keycloak propagation | `docs/test-evidence/api-backed-channel-smoke.md`, `docs/test-evidence/keycloak-live-realm-smoke.md` | live-keycloak-backed |
| audit-console | Audit retention parameters: `AUD-201` | yes | manifest renderer with API-backed endpoints | yes | yes; `GET /api/staff/audit-parameters`, history, and change request | `audit_retention_parameters`, `audit_retention_parameter_versions`, `parameter_change_requests` | staff/audit route policy plus service role checks | parameter view/change/apply audit | yes | `ParameterAdminIntegrationTest` and `npm run test:parameter-command-e2e-compose` passed locally | `docs/test-evidence/parameter-admin-apis.md` | api-backed-command |
| audit-console | Notification delivery history: `AUD-301` | yes | yes; API-backed smoke panel reads notification delivery history | yes | yes; `GET /api/notifications/deliveries` with reason-required filters | notification delivery requests and delivery/preference access audit tables | auditor/compliance/ops notification route policy | `NOTIFICATION_DELIVERY_HISTORY_VIEW` access audit with masked message payloads | not-applicable | manifest validation, audit-console typecheck, static Next scaffold coverage, and targeted Playwright parity passed locally | `docs/test-evidence/notification-service.md` | api-backed-read |
| audit-console | Reporting artifact history: `AUD-401` | yes | yes; API-backed panel reads reporting artifact status, checksum, export format, retention metadata, and workflow timeline | yes; `reportArtifacts` method | yes; `GET /api/reports/artifacts` | `report_artifacts`, `reporting_workflow_instances`, `reporting_workflow_events`, plus `reporting_access_audit_events`; artifacts include rendered JSON payload checksum, synthetic retention metadata, and workflow lifecycle references | auditor/compliance/reporting analyst route policy | `REPORT_ARTIFACT_LIST_VIEW` audit with `maskedByDefault` and `syntheticOnly` metadata; workflow timeline rows record actor, role, reason, and status transition | not-applicable | reporting-service API client typecheck, audit-console typecheck, manifest validation, static scaffold coverage, reporting-service integration test, and conditional Playwright reporting smoke skipped locally without reporting URL | `docs/test-evidence/reporting-service.md`, `docs/test-evidence/api-backed-channel-smoke.md` | api-backed-read |
| fds-aml-console | FDS/AML case dashboards and decisions: `FDS-101`, `FDS-201`, `FDS-202`, `AML-101`, `AML-201` | yes | yes | yes | yes | FDS/AML and approval tables | reviewer/checker roles | workflow audit | yes | synthetic runtime, Spring integration, browser command/failure smokes, live FDS/AML Keycloak propagation | `docs/test-evidence/fds-aml-reconciliation.md`, `docs/test-evidence/keycloak-live-realm-smoke.md` | live-keycloak-backed |
| fds-aml-console | FDS rule parameters: `FDS-301` | yes | manifest renderer with API-backed endpoints | yes | yes; `GET /api/staff/fds-parameters`, history, and change request | `fds_rule_parameters`, `fds_rule_parameter_versions`, `parameter_change_requests`; `CustomerTransferService` reads `highAmountMinor` | staff/FDS route policy plus service role checks | parameter view/change/apply audit | yes | `ParameterAdminIntegrationTest` passed locally and proves changed threshold changes held-transfer behavior; `npm run test:parameter-command-e2e-compose` passed the live browser command path | `docs/test-evidence/parameter-admin-apis.md` | api-backed-command |
| admin-console | Platform control dashboard: `ADM-101` | yes | yes | yes | yes | reads configured target-state evidence | compliance/passkey role policy | admin access audit path partial | not-applicable | Spring integration, browser read smoke, live `security-admin01` Keycloak propagation | `docs/test-evidence/api-backed-channel-smoke.md`, `docs/test-evidence/keycloak-live-realm-smoke.md` | live-keycloak-backed |
| admin-console | Security policy and menu/role parameters: `ADM-201`, `ADM-301` | yes | manifest renderer with API-backed endpoints | yes | yes; security and authorization parameter current/history/change APIs with structured `ADM-301` value validation | `security_policy_parameters`, `security_policy_parameter_versions`, `menu_role_parameters`, `menu_role_parameter_versions`, `parameter_change_requests` | admin route policy plus service role checks | parameter view/change/apply audit | yes | `ParameterAdminIntegrationTest` passed locally and rejects invalid menu-role value shapes before approval creation; `npm run test:parameter-command-e2e-compose` passed both live admin browser command paths | `docs/test-evidence/parameter-admin-apis.md` | api-backed-command |
| admin-console | Notification template approval and preference management: `ADM-401`, `ADM-402` | yes | yes; API-backed smoke panel reads templates, template workflow timelines, and preferences | yes | yes; template change-request list/approval/rejection and preference list/upsert APIs | notification templates, template change requests, notification workflow instances/events, recipient preferences, suppression audit, and access audit tables | ops/compliance notification route policy; template activation requires independent checker; workflow list is ops/audit/compliance scoped | template maker/checker/reason audit, template workflow requested/terminal events, preference view/change audit, masked recipient identifiers | yes for template approval; no maker-checker for routine preference changes | `NotificationTemplateAdminIntegrationTest`, `NotificationAuthorizationIntegrationTest`, admin-console typecheck, packages typecheck, static Next scaffold coverage, manifest validation, and targeted Playwright parity passed locally | `docs/test-evidence/notification-service.md` | api-backed-command |
| admin-console | Reporting artifact management: `ADM-701` | yes | yes; API-backed panel reads report catalog, generates rendered synthetic JSON artifacts, lists generated artifacts, simulates JSON package export with checksum/retention metadata, and shows artifact workflow state/timeline; manifest declares retention sweep command | yes; `reportCatalog`, `generateReportArtifact`, `reportArtifacts`, `exportReportArtifact`, and `runReportRetentionSweep` methods | yes; `GET /api/reports/catalog`, `POST /api/reports/artifacts`, `GET /api/reports/artifacts`, `GET /api/reports/artifacts/{artifactId}/export`, and `POST /api/reports/retention/sweeps` | `report_definitions`, `report_artifacts`, `reporting_workflow_instances`, `reporting_workflow_events`, `reporting_access_audit_events`, and `reporting_outbox_events`; `artifact_content`, `content_sha256`, `retention_policy`, `retention_until`, `export_format`, `expired_at`, and `retention_action` persist rendered synthetic payload, lifecycle controls, and workflow references; outbox rows persist generated/exported/retention-completed events as `PENDING` until the Kafka publisher marks broker-acknowledged rows `PUBLISHED` | reporting analyst/compliance route policy with signed JWKS JWTs or explicitly enabled local simulator tokens; retention sweep is service-layer restricted to compliance, ops, or reporting analyst actors | `REPORT_CATALOG_VIEW`, `REPORT_GENERATED`, `REPORT_GENERATE_REPLAYED`, `REPORT_ARTIFACT_LIST_VIEW`, `REPORT_ARTIFACT_EXPORTED`, and `REPORT_RETENTION_SWEEP_RUN` reason audit; workflow events record generated/exported/expired actor, role, reason, and status transition; Redpanda envelopes carry `syntheticOnly=true`, `sourceService=reporting-service`, and `ledgerRowsMutated=false` controls | not-applicable for synthetic report generation | reporting-service API client typecheck, admin-console typecheck, manifest validation, static scaffold coverage, reporting-service integration test, Redpanda publisher integration test, live Keycloak service-token export/retention smoke, and conditional Playwright reporting smoke skipped locally without reporting URL | `docs/test-evidence/reporting-service.md`, `docs/test-evidence/api-backed-channel-smoke.md` | api-backed-command |
| admin-console | Platform evidence coverage: `ADM-501` | yes | yes; API-backed panel reads curated evidence and feature coverage metadata | yes | yes; `GET /api/admin/platform/evidence-coverage` | audit hash-chain event only; no source-row mutation | admin route policy plus service role checks | reason-required `ADMIN_EVIDENCE_COVERAGE_VIEW` audit | not-applicable | `AdminPlatformApiParityIntegrationTest`, admin-console typecheck, manifest validation, and static scaffold coverage | `docs/test-evidence/admin-platform-evidence-coverage.md`, `docs/implementation-coverage-matrix.md` | api-backed-read |
| admin-console | System and batch status: `ADM-601` | yes | yes; API-backed panel reads service, batch, and monitoring metadata | yes | yes; `GET /api/admin/platform/system-status` | reads `daily_closings`, `eod_closing_steps`, `interest_posting_batches`, `fee_posting_batches`, and `outbox_events` | admin route policy plus service role checks | reason-required `ADMIN_SYSTEM_STATUS_VIEW` audit | not-applicable | `AdminPlatformApiParityIntegrationTest`, admin-console typecheck, manifest validation, and static scaffold coverage | `docs/test-evidence/admin-platform-system-status.md` | api-backed-read |
| core-banking | Ledger deposit/withdrawal/transfer/reversal/adjustment/closing | not-applicable | not-applicable | partial channel clients | yes | yes | API role filter for ledger/staff/customer paths | ledger audit/outbox | adjustment approval where routed through ops | JUnit unit passes; targeted integration evidence exists; full integration depends on Docker availability | `docs/test-evidence/phase-2-ledger-core.md` | api-backed-command |
| core-banking | Loan domain application, underwriting, execution, schedule, accrual, repayment, prepayment | not-applicable | not-applicable | yes | yes | `V022__loan_domain.sql` plus append-only `LOAN_*` ledger postings and outbox events | loan route policy plus customer ownership and staff reason checks | loan application/execution/accrual/payment audit | yes for execution | `LoanDomainIntegrationTest` passed locally after sandbox escalation | `docs/test-evidence/loan-domain.md` | api-backed-command |
| core-banking | Card domain issue, 3DS simulation, authorization hold, capture, cancellation, reversal, limits, lost-card status | not-applicable | not-applicable | yes | yes | `V023__card_domain.sql` plus append-only `CARD_CAPTURE` and `REVERSAL` ledger postings and outbox events | card route policy plus customer ownership checks | card issue/3DS/authorization/capture/cancel/reversal/loss audit | not-applicable for first customer self-service lifecycle slice | `CardDomainIntegrationTest` passed locally after sandbox escalation | `docs/test-evidence/card-domain.md` | api-backed-command |
| core-banking | Structured errors, audit hash chain, approval API, outbox, Temporal references | not-applicable | not-applicable | yes for active channel paths | yes | yes | yes | yes | yes for high-risk workflows | Node/JUnit/conditional E2E; integration needs Docker for full local rerun | `docs/test-evidence/evidence-gap-report.md` | api-backed-command |
| payment-service | Synthetic bill payment instruction, idempotency, cancellation, settlement reference, durable outbox dispatch and configurable scheduled worker, autopay agreements/execution, core-banking bill-payment posting bridge, API-backed staff cancellation maker-checker correction flow, AsyncAPI cancellation and instruction lifecycle event contracts, Redpanda payment domain event publisher and scheduled publisher worker, and Compose/Kubernetes/Helm API plus worker runtime wiring; manifests `CWB-701`, `CWB-702`, `CWB-703`, `PAY-101`, `PAY-102`, `OPS-404` | yes | customer-web includes an API-backed payment panel for bill-payment idempotent replay, instruction read/outbox visibility, and autopay create/pause/resume/cancel; staff-terminal includes API-backed PAY-101 reason-required payment instruction inquiry and PAY-102 payment cancellation maker-checker approval; ops-console includes API-backed OPS-404 payment Outbox dispatch | yes; `createPaymentInstruction`, reason-aware read/cancel/settle, staff cancellation request/approve/reject, dispatch, and autopay create/read/pause/resume/cancel/due execution client methods | yes; `POST /api/payments/instructions`, reason-required staff read, customer self-cancel, settlement, staff cancellation request/approve/reject maker-checker APIs, autopay create/read/pause/resume/cancel/due execution APIs, `POST /api/payments/outbox/ledger-postings/dispatch-next`, scheduled ledger worker runner, scheduled payment-domain publisher worker, payment-domain Kafka publisher service, Keycloak client-credentials token provider for the core-banking posting bridge, and `POST /api/ledger/payment-postings` for service-to-service settlement | payment-service Flyway schema for billers, instructions, attempts, status history, idempotency, durable payment Outbox events, dispatch retry/dead-letter state, `payment_autopay_agreements`, status history, executions, `payment_cancellation_requests`, and `payment_access_audit_events`; payment-domain Kafka publisher marks non-ledger outbox rows `PUBLISHED` only after broker acknowledgement; core-banking `BILL_PAYMENT` ledger transaction with `PAYMENT` postings to `BANK-SETTLEMENT`; Compose, raw Kubernetes, and Helm runtimes use `payment_flyway_schema_history` on the shared synthetic PostgreSQL database | payment-service route policy validates signed JWKS JWTs or dev-only simulator tokens; instruction create/autopay/customer cancel commands require `CUSTOMER`, staff/ops/audit instruction reads require role plus business reason audit, direct staff/ops payment cancel is denied on the customer cancel route, staff/ops correction creation and independent manager approval/rejection are role-gated and actor-bound, settlement/dispatch/due execution require `PAYMENT_SERVICE` or ops roles; core ledger posting API is restricted to `PAYMENT_SERVICE`/ops operator roles; commands require requested actor and reason; autopay rejects non-synthetic billers; Compose/Kubernetes/Helm use the `payment-service-api` audience, Keycloak token URL/client-secret wiring, and Redpanda synthetic publisher settings | payment status history records create, customer cancel, checker-approved staff cancel, settle, dispatcher settlement, and terminal dispatch failure actors/reasons; staff cancellation requests persist `PCR-*` maker/checker rows with separation of duties; PAY-101 staff reads append `PAYMENT_INSTRUCTION_VIEW` access audit with `PAU-*`; PAY-102 declares reason-required cancellation request/approve/reject audit metadata; `payment.instruction.canceled`, `payment.instruction.settled`, `payment.instruction.failed`, `payment.instruction.retry.scheduled`, and `payment.instruction.dead-lettered` AsyncAPI schemas map lifecycle payloads with synthetic-only, no-real-network, and no-direct-ledger-write controls; dispatch failure now emits `PaymentInstructionFailed` plus `PaymentInstructionRetryScheduled`, while terminal dispatch failure moves the instruction to `FAILED` and emits `PaymentInstructionDeadLettered`; Redpanda publisher envelopes add synthetic-only and `sourceService=payment-service` headers; OPS-404 dispatch records requested actor/reason and retry/dead-letter result; autopay status history records create/pause/resume/cancel; core settlement writes `PaymentLedgerPostingSettled` Outbox event; autopay execution writes `PaymentAutopayExecutionCreated`; manifests require payment lookup/command audit reasons; worker deployments set `payment-compose-outbox-worker`, `payment-k8s-outbox-worker`, `payment-helm-outbox-worker`, `payment-compose-domain-event-publisher`, `payment-k8s-domain-event-publisher`, or `payment-helm-domain-event-publisher` actors/client ids | yes; staff payment cancellation now requires maker request plus independent checker approval before mutation | `PaymentInstructionIntegrationTest`, `PaymentOutboxDispatcherIntegrationTest`, `PaymentOutboxWorkerIntegrationTest`, `PaymentKafkaOutboxPublisherIntegrationTest`, `LivePaymentDomainEventPublisherComposeSmokeIntegrationTest`, `PaymentDomainEventPublisherWorkerTest`, `PaymentAutopayIntegrationTest`, `PaymentAuthorizationIntegrationTest`, `LedgerCommandServiceIntegrationTest`, and `LedgerRuntimeApiParityIntegrationTest` passed locally after sandbox escalation; `npm run test:payment-service:unit`, `npm run test:payment-service:unit -- --tests lab.banking.payment.core.CoreBankingServiceTokenProviderTest --rerun-tasks`, `npm run test:payment-service:integration -- --tests lab.banking.payment.PaymentOutboxDispatcherIntegrationTest --rerun-tasks`, `npm run test:payment-service:integration -- --tests lab.banking.payment.PaymentAuthorizationIntegrationTest --rerun-tasks`, `npm run test:payment-service:integration -- --tests lab.banking.payment.PaymentKafkaOutboxPublisherIntegrationTest --rerun-tasks`, `npm run test:payment-service:integration -- --tests lab.banking.payment.LivePaymentDomainEventPublisherComposeSmokeIntegrationTest --rerun-tasks`, `npm run test:payment-service:integration -- --tests lab.banking.payment.LivePaymentOutboxWorkerComposeSmokeIntegrationTest --rerun-tasks`, `npm run test:payment-service:domain-publisher-compose`, `npm run test:payment-service:outbox-worker-compose`, `npm run test:core-banking:unit -- --tests lab.banking.core.security.KeycloakRealmPolicyTest --rerun-tasks`, `npm run test:core-banking:integration -- --tests lab.banking.core.security.JwksAuthorizationIntegrationTest --rerun-tasks`, and full `npm run test:payment-service:integration -- --rerun-tasks` passed after sandbox escalation; `docker compose --profile platform config`, `npm run k8s:validate`, `npm run helm:template`, `npm run security:posture-check`, `npm run validate:manifests`, `npm run packages:typecheck`, `npm test`, `node --test tests/springScaffold.test.mjs`, customer/staff/ops Next typechecks, and targeted customer/staff/ops Playwright parity passed for channel contracts | `docs/test-evidence/payment-service.md`, `contracts/openapi/payment-service.yaml`, `contracts/events/payment-ledger-posting-requested.schema.json`, `contracts/events/payment-ledger-posting-settled.schema.json`, `contracts/events/payment-autopay-execution-created.schema.json`, `contracts/events/payment-instruction-canceled.schema.json`, `contracts/events/payment-instruction-settled.schema.json`, `contracts/events/payment-instruction-failed.schema.json`, `contracts/events/payment-instruction-retry-scheduled.schema.json`, `contracts/events/payment-instruction-dead-lettered.schema.json` | partial |
| notification-service | Synthetic notification event consumption, Redpanda outbox envelope consumer/worker, live Compose consumer smoke, live provider retry/dead-letter smoke, live Keycloak service-token smoke, masked delivery requests/history, recipient preferences, suppression audit, retry, dead-letter, template administration, customer self-service preference/history management, notification delivery lifecycle AsyncAPI contracts, and raw Kubernetes/Helm API plus worker deployment manifests | yes; `CWB-801`, `CWB-802`, `ADM-401`, `ADM-402`, and `AUD-301` cover customer preference/history self-service, template approval, admin preference management, and delivery-history inquiry | customer/admin/audit API-backed smoke panels for owned customer preference/history, template/preference, and delivery-history flows | yes; consume/history/read/failure/delivered/template change-request/admin preference/customer preference/customer delivery-history methods in `@banking-lab/api-client` | yes; event consume, admin delivery history/read, customer owned delivery history, failure, delivered, template list, template change request, approval, rejection, admin preference list/upsert, customer owned preference list/upsert APIs, Kafka consumer batch path, disabled-by-default event-consumer worker, Docker Compose API/consumer runtime configuration, live Compose Redpanda-to-delivery smoke, live Compose provider failure-to-dead-letter smoke, live `notification-service-api` Keycloak service-token smoke, notification delivery requested/attempted/delivered/failed/dead-lettered AsyncAPI channels, and structural Kubernetes/Helm API/worker deployment validation | notification-service Flyway schema for templates, template change requests, recipient preferences, delivery/preference access audit, suppressed-event audit, inbox idempotency, delivery requests, attempts, and dead letters; live provider smoke verifies durable `notification_dead_letters` rows and failure/dead-letter attempts; Compose, raw Kubernetes, and Helm runtimes use `notification_flyway_schema_history` to avoid core-banking Flyway version conflicts on the shared synthetic PostgreSQL database | notification-service route policy validates signed JWKS JWTs or dev-only simulator tokens; event consumption requires service/outbox roles, delivery mutation requires `NOTIFICATION_SERVICE` or ops roles, admin delivery history/read is ops/audit/compliance scoped, template creation is ops/compliance scoped, template approval/rejection is manager/compliance scoped, admin preference mutation/read is ops/compliance/audit scoped, customer preference/history self-service is CUSTOMER scoped and enforces token `customerId` ownership, Kafka consumer rejects non-synthetic envelopes before delivery side effects, and Keycloak issues `notification-service-api` client-credentials tokens with only the `NOTIFICATION_SERVICE` realm role and `notification-service-api` audience; Compose, raw Kubernetes, and Helm disable real providers explicitly and use `notification-service-api` | delivery attempts record requested actor/reason and failure text; lifecycle event schemas require `syntheticOnly=true`, `realProviderUsed=false`, and `realPiiExposed=false`; provider dead-letter smoke proves terminal deliveries reject delivered-state mutation through structured `NOTIFICATION_STATE_TRANSITION_REJECTED`; admin delivery history reads append `NOTIFICATION_DELIVERY_HISTORY_VIEW`; customer delivery history reads append `NOTIFICATION_CUSTOMER_DELIVERY_HISTORY_VIEW`; template change requests record maker/checker/reason and terminal decision state; admin preferences record actor/reason, preference reads append `NOTIFICATION_PREFERENCE_VIEW`, customer preference reads append `NOTIFICATION_CUSTOMER_PREFERENCE_VIEW`, and suppression rows store masked payload JSON; live Compose worker records `notification-compose-event-consumer`; messages and account aliases are masked; Prometheus scrapes API and consumer worker metrics | yes; template change requests require reason, independent checker, checker role, and terminal approval/rejection before activation | `NotificationDeliveryIntegrationTest`, `NotificationAuthorizationIntegrationTest`, `NotificationTemplateAdminIntegrationTest`, `NotificationPreferenceIntegrationTest`, and `NotificationKafkaConsumerIntegrationTest` passed locally with PostgreSQL/Redpanda Testcontainers; `NotificationEventConsumerWorkerTest`, unit compile, manifest validation, Next customer/admin/audit typechecks, targeted customer/admin/audit Playwright parity, api-client/package typecheck, notification lifecycle scaffold contract coverage, `docker compose --profile platform config`, live Compose notification consumer smoke, `npm run test:notification-service:keycloak-service-token`, `npm run test:notification-service:provider-dead-letter-compose`, `npm run k8s:validate`, `npm run helm:template`, `npm run security:posture-check`, `npm test`, `npm run evidence:refresh-check`, and `npm run node:retirement-gate` passed | `docs/test-evidence/notification-service.md`, `contracts/openapi/notification-service.yaml`, `contracts/events/notification-delivery-requested.schema.json`, `contracts/events/notification-delivery-attempted.schema.json`, `contracts/events/notification-delivery-delivered.schema.json`, `contracts/events/notification-delivery-failed.schema.json`, `contracts/events/notification-delivery-dead-lettered.schema.json` | partial |
| notification-service | Template change workflow visibility for notification template administration | yes; `ADM-401` surfaces workflow state in the admin API-backed panel | yes; admin-console reads template workflow list and timeline events | yes; `listNotificationTemplateChangeRequests` plus workflow status/timeline DTOs | yes; `GET /api/notifications/templates/change-requests` lists change requests with workflow metadata, and approve/reject returns terminal workflow state | `notification_workflow_instances`, `notification_workflow_events`, and `notification_template_change_requests.workflow_instance_id` persist Temporal-compatible workflow references and timeline events | ops/audit/compliance can list workflow state; CUSTOMER route access is rejected; approval/rejection remains manager/compliance scoped | `REQUESTED`, `APPROVED`, and `REJECTED` workflow events record actor/reason/status without copying template bodies or delivery payloads | yes; activation still requires independent checker and terminal approval before template use | `NotificationTemplateAdminIntegrationTest`, `NotificationAuthorizationIntegrationTest`, `npm run packages:typecheck`, `npm run next:admin-console:typecheck`, and `node --test tests/nextScaffold.test.mjs` passed locally after workflow visibility update | `docs/test-evidence/notification-service.md` | api-backed-command |
| reporting-service | Synthetic report catalog, rendered JSON artifact payloads, checksum/retention metadata, package export simulation, retention lifecycle expiration, artifact workflow lifecycle visibility, durable reporting outbox persistence, Redpanda/Kafka publisher coverage, retry/dead-letter state handling, live Compose publisher smoke, and Compose/Kubernetes/Helm publisher worker runtime wiring for audit, operations, and evidence coverage reports plus live Keycloak service-token smoke and admin/audit channel UI wiring | `ADM-701`, `AUD-401` | admin-console and audit-console API-backed panels for catalog, rendered artifact generation, artifact history, checksum, retention metadata, export package metadata, and artifact workflow timeline; retention sweep is manifest-declared | yes; reporting catalog, generate artifact, list artifact, export artifact, and run retention sweep methods in `@banking-lab/api-client` with workflow DTOs | yes; `GET /api/reports/catalog`, `POST /api/reports/artifacts`, `GET /api/reports/artifacts`, `GET /api/reports/artifacts/{artifactId}/export`, and `POST /api/reports/retention/sweeps`; disabled-by-default `ReportingDomainEventPublisherWorker` publishes allow-listed reporting outbox events when configured with retry delay and dead-letter threshold | `report_definitions`, `report_artifacts`, `reporting_workflow_instances`, `reporting_workflow_events`, `reporting_access_audit_events`, and `reporting_outbox_events` with idempotency key uniqueness, durable `PENDING` outbox rows that transition to `PUBLISHED` after broker acknowledgement or to `FAILED`/`DEAD_LETTER` with `retry_count`, `next_retry_at`, and `error_message`, `artifact_content` JSONB, `content_sha256`, `retention_policy`, `retention_until`, `export_format`, `expired_at`, `retention_action`, masked-by-default controls, workflow event rows, and dedicated Compose/Kubernetes/Helm Flyway history configuration for both API and publisher worker runtimes | reporting route policy validates signed JWKS JWTs or explicitly enabled dev simulator tokens; access is limited to auditor, compliance, ops manager, and reporting analyst roles, while retention sweep service logic rejects non-compliance/non-ops/non-reporting actors; Keycloak issues `reporting-service-api` client-credentials tokens with only the `REPORTING_ANALYST` realm role and `reporting-service-api` audience | reason-required catalog/list/generate/export/retention audit rows with replay audit for duplicate idempotency keys; generated/export/expired workflow events record actor, role, reason, and status transitions; generated/export audit payloads record checksum, package name, and retention policy; retention audit proves `ledgerRowsMutated=false`; domain events persist as outbox rows, and Redpanda envelopes carry `syntheticOnly=true`, `sourceService=reporting-service`, aggregate metadata, and payload controls; broker failures leave `published_at` empty, preserve bounded error text, and either schedule retry or enter `DEAD_LETTER`; API runtime disables publisher mode while the worker runtime uses a dedicated OTEL service name/client id and live Compose smoke proves restart-to-publish behavior | not-applicable for synthetic report generation | `ReportingServiceIntegrationTest`, `ReportingKafkaOutboxPublisherIntegrationTest` for Redpanda publish plus retry/dead-letter, `npm run test:reporting-service:domain-publisher-compose`, Compose platform config, Kubernetes/Helm structural validation, security posture check, `npm run test:reporting-service:keycloak-service-token` with export and retention endpoint coverage, reporting API client typecheck, admin/audit Next typechecks, manifest validation, static scaffold coverage, and conditional Playwright admin/audit reporting smokes skipped locally without reporting URL | `docs/test-evidence/reporting-service.md`, `docs/test-evidence/api-backed-channel-smoke.md`, `docs/test-evidence/platform-deployment-validation.md` | api-backed-command |
| product-ledger | Deposit product catalog, rates, interest accrual/posting batches | yes | staff manifest renderer and ops manifest renderer | yes | yes | product/rate/enrollment/accrual/batch migrations plus `INTEREST` posting type | product route, staff route, ops route policy | product parameter and interest batch audit | rate change yes; interest batch no in first slice | `DepositProductApiIntegrationTest` passed locally | target goal Phase C | api-backed-command |
| product-ledger | Fee policy, fee policy versioning, fee posting, fee refund | yes | staff/ops manifest renderer | yes | yes | fee policy/version/change/batch migrations plus refund reversal reference | fee read/staff/ops route and service policy | fee view, parameter approval, posting, and refund audit | policy changes and targeted refunds through fee waiver approval | `FeePolicyApiIntegrationTest` passed locally | target goal Phase C | api-backed-command |
| analytics | Python AML/FDS scoring, DuckDB mart, exports, console/evidence linkage | not-applicable | FDS/AML console reads generated scoring evidence through `AnalyticsEvidencePanel` using the Spring API client | yes; `fdsAnalyticsEvidence(reason)` calls `/api/fds/analytics` | yes; `GET /api/fds/analytics` reads the generated artifact, requires reason, validates synthetic controls, and audits the view | DuckDB in-memory mart, sample transactions, JSON/CSV export; Spring reads the generated artifact only | FDS/AML/audit/compliance role policy plus service role checks | `FDS_ANALYTICS_EVIDENCE_VIEW` audit with screen `FDS-301` | not-applicable | `FdsAnalyticsEvidenceIntegrationTest`, `next:fds-aml-console:typecheck`, `packages:typecheck`, and `tests/nextScaffold.test.mjs` passed locally | `docs/test-evidence/fds-analytics-spring-read-api.md`, `docs/test-evidence/generated/fds-aml-analytics.json` | complete |
| formal | Executable ledger and idempotency model checking gate | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | `npm run formal:ledger` passed with actual TLC for `formal/Ledger.tla` and `formal/Idempotency.tla` through the supplied `~/Downloads/tla2tools.jar`, plus bounded state-search over 3335 states, 8241 transitions, and 15 invariants; static-only remains rejected for CI/milestone evidence | `docs/test-evidence/generated/formal-ledger-tlc-result.json`, `docs/test-evidence/formal-ledger-verification.md` | complete |
| platform | Kubernetes/Helm/Argo CD structural validation | k8s/helm manifests | not-applicable | not-applicable | not-applicable | structural manifests for PostgreSQL, core-banking, payment-service, payment outbox worker, payment domain-event publisher, reporting-service, reporting domain-event publisher, notification-service, notification event-consumer worker, Keycloak, Redpanda, Temporal, TLS Ingress, example-only TLS secret placeholders, Argo CD Applications, and network policy intent | not-applicable | not-applicable | not-applicable | `npm run platform:validate` passed; `npm run k8s:validate` passed structural validation for 27 resources with Keycloak management-port health probes, `npm run helm:template` passed with the Helm CLI renderer for 27 rendered resources, and `npm run argocd:validate` passed for 2 Argo CD Applications | `docs/test-evidence/platform-deployment-validation.md`, `docs/test-evidence/generated/k8s-validation.json`, `docs/test-evidence/generated/helm-template-validation.json`, `docs/test-evidence/generated/argocd-validation.json` | complete |
| platform | Live cluster deployment validation | k8s/helm manifests | not-applicable | not-applicable | not-applicable | disposable kind cluster plus Helm chart runtime for PostgreSQL, Keycloak, Redpanda, Temporal, and core-banking with payment/reporting/notification replicas scaled to zero for this scoped smoke | not-applicable | not-applicable | not-applicable | `npm run platform:live-runtime-smoke` runs `kind create cluster`, builds and loads the local core-banking image, installs the scoped Helm runtime, waits for PostgreSQL, Keycloak, Redpanda, Temporal, and core-banking rollouts, checks core `/health`, Keycloak management readiness, Redpanda admin readiness, Temporal endpoints, TLS Ingress object, and synthetic TLS Secret type; Argo CD controller sync is explicitly skipped when no CRD/controller is present | `docs/test-evidence/platform-deployment-validation.md`, `docs/test-evidence/generated/k8s-live-deployment-validation.json`, `docs/test-evidence/generated/platform-live-runtime-smoke.json` | complete |
| security-evidence | SCA/SAST/Trivy/SBOM/DAST evidence and Docker rerun path | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | not-applicable | `BANKING_LAB_DAST_URL=http://host.docker.internal:18132/health npm run security:evidence:docker` passed against a disposable synthetic core-banking target with Docker-forced Semgrep, Trivy, CycloneDX SBOM, and ZAP baseline evidence; generated summary records 5 passed, 0 failed, 0 skipped | `docs/test-evidence/generated/security-evidence-summary.json`, `docs/test-evidence/generated/zap-baseline.json`, `docs/test-evidence/generated/zap-baseline.log`, `docs/test-evidence/security-docker-rerun.md` | complete |
| operations-evidence | Phase G synthetic load smoke | no | no | no | local synthetic handler runner only | not-applicable | not-applicable | load smoke validates audit hash chain | not-applicable | `npm run load:synthetic` and `tests/loadEvidence.test.mjs` pass | `docs/test-evidence/load-test-summary.md`, `docs/test-evidence/generated/load-test-summary.json` | complete |
| operations-evidence | PostgreSQL backup/restore drill contract and live restore path | no | no | no | live mode is executed by `npm run postgres:backup-drill:docker-live`, which invokes the canonical `npm run postgres:backup-drill -- --mode=live` against disposable source/restore PostgreSQL URLs | live `pg_dump --format=custom`, `pg_restore --clean --if-exists`, and `psql` verification over migrated synthetic schema | not-applicable | live restore validates audit hash-chain continuity | not-applicable | `npm run postgres:backup-drill:docker-live` passed locally with `postgresLive=true`, ledger transaction/posting/count parity, `account_balance_projections == Σ signed postings`, customer availability checks, audit hash-chain continuity, approval/workflow/customer-transfer-result parity, and synthetic-boundary proof | `docs/test-evidence/postgres-backup-restore-drill.md`, `docs/test-evidence/generated/postgres-backup-restore-drill.json` | complete |
| hardening-h1 | Spring canonical lock and synthetic runtime guard | not-applicable | target apps remain Next.js/React source | shared API/auth/screen packages remain TypeScript target contracts | `services/core-banking` remains the Spring canonical backend | Flyway/PostgreSQL target path only; synthetic runtime helpers are not counted as target services | target auth stack anchors include signed JWT/JWKS and Keycloak | retirement audits and parity evidence retained | not-applicable | `node --test tests/stackRetirementAreaAudit.test.mjs`, `npm run node:retirement-gate`, and `npm run parity` passed; node gate now invokes stack-area audit and rejects an injected target-path `runtime/synthetic-reference` dependency canary | `docs/test-evidence/hardening-h1-spring-canonical-lock.md`, `docs/test-evidence/stack-retirement-area-audit.md` | complete |
| hardening-h2 | Secure-default auth, step-up, trusted-device, and session-revocation controls | not-applicable | protected channel calls continue to use JWT/Keycloak paths; `/api/auth/session` exposes synthetic session/device status | existing protected API clients remain compatible with Bearer tokens; new session endpoint is Spring-backed | yes; security defaults on, simulator tokens double opt-in, JWKS path defaulted, high-risk step-up/device/session policy enforced before domain execution | `V026__security_auth_hardening_controls.sql`, `trusted_devices`, `revoked_sessions` | Keycloak/JWKS default path, simulator fallback disabled by default, MFA/WebAuthn `amr`/`acr` step-up, trusted-device and revoked-session gates | structured auth denial audit includes `STEP_UP_REQUIRED`, `TRUSTED_DEVICE_REQUIRED`, `SESSION_REQUIRED`, `SESSION_EXPIRED`, and `SESSION_REVOKED` | high-risk approval/ops/PII/parameter routes are denied before checker/domain mutation when fresh step-up is absent | `npm run security:posture-check`, `SecurityDefaultsIntegrationTest`, `JwksAuthorizationIntegrationTest`, full Spring integration, unit, manifest, screen-engine, formal ledger, security evidence, and node retirement gates passed locally | `docs/test-evidence/hardening-h2-secure-auth.md`, `docs/test-evidence/generated/security-posture-check.json` | complete |
| hardening-h3 | DB-level ledger integrity, chart of accounts, system-account routing, and partition-route evidence | not-applicable | not-applicable | ledger API clients unchanged | yes; posting service still appends transactions/postings and now routes interest/fee/loan-interest through typed system accounts | `V027__ledger_db_integrity_and_accounting_structure.sql` adds deferred balance triggers, `account_class`, `system_account_kind`, seeded synthetic system accounts, and partitioned route tables for transaction/posting business dates | inherited from H2 route policy; no new auth surface | ledger source rows remain append-only and direct fixture seeds now commit balanced source rows atomically | not-applicable for DB invariant enforcement; maker-checker paths unchanged | `npm run ledger:integrity-check`, `LedgerCommandServiceIntegrationTest`, product/loan/card regression, statement/FDS/reconciliation/EOD/parameter/staff fixture regression, and `npm run formal:ledger` passed locally | `docs/test-evidence/hardening-h3-ledger-db-integrity.md` | complete |
| hardening-h4 | HA/DR proof with multi-instance ledger concurrency, fault-drill aggregation, and live backup/restore rerun | not-applicable | not-applicable | no API contract changes; direct ledger withdrawal now has bounded retry for retryable serialization failures | yes; two independent Spring contexts share one PostgreSQL ledger and converge without duplicate postings | no new migration; V027 backup/restore evidence refreshed with 10 restored balance projections | inherited from H2 route policy; H4 drill runs synthetic internal service path with security disabled only inside test context | RPO 0 evidence for ledger, audit hash-chain, approval, workflow, customer-transfer result, and outbox/Temporal restart drills | not-applicable for new command approval; existing restart drills preserve maker-checker workflow completion | `npm run dr:multi-instance-drill`, `npm run postgres:backup-drill:docker-live`, and `LedgerCommandServiceIntegrationTest` passed locally after fixing the first exposed serialization retry gap | `docs/test-evidence/hardening-h4-ha-dr-proof.md`, `docs/failure-drills/hardening-h4-ha-dr-rto-rpo.md`, `docs/test-evidence/generated/ha-dr-multi-instance-drill.json`, `docs/test-evidence/generated/postgres-backup-restore-drill.json` | complete |
| hardening-h5 | Operational-security lab with WORM audit export, synthetic KMS, PAM break-glass, and SIEM alert drill | not-applicable | ops/security APIs are target Spring endpoints; no hand-coded screen expansion in this slice | no public channel API contract changes; `/api/ops/security/*` adds ops/admin control APIs | yes; H5 controls are Spring/PostgreSQL-backed and use audit hash-chain appenders | `V028__operational_security_lab_controls.sql` adds `synthetic_kms_keys`, `audit_worm_export_segments`, `break_glass_grants`, and `break_glass_review_cases` | `/api/ops/security/*` is role-gated and POST commands require fresh step-up; no real secrets or real key material stored | WORM segment hashes detect tampering; synthetic KMS rotation preserves old segment verification; SIEM rules are synthetic Loki/local drill rules | break-glass grants are time-boxed and require independent post-hoc review; self-review is rejected | `npm run siem:alert-drill` and `OperationalSecurityIntegrationTest` passed locally after enabling step-up enforcement in the test context | `docs/test-evidence/hardening-h5-operational-security.md`, `docs/test-evidence/generated/siem-alert-drill.json`, `docs/security/synthetic-secret-rotation-runbook.md` | complete |
| hardening-h6 | AML/FDS depth with sanctions-sim, PEP-sim, model-card lineage, synthetic STR, and regulatory reporting | not-applicable | FDS/AML governance evidence is Spring/API backed; no screen expansion in this slice | no public channel API contract changes; `/api/aml/governance/*` adds AML governance APIs | yes; screening, disposition, and model-card reads are Spring/PostgreSQL-backed | `V029__aml_fds_governance_reporting.sql` adds `synthetic_watchlist_entries`, `sanctions_screening_hits`, `aml_model_versions`, `aml_case_evidence_packages`, `synthetic_str_reports`, and `synthetic_aml_regulatory_reports` | `/api/aml/governance/*` is role-gated and POST commands require fresh step-up; artifacts assert no real sanctions, PII, regulator, payment-network, or external FI data | screening hit and false-positive disposition audit rows are appended; generated model/STR/regulatory artifacts include source hash lineage | false-positive disposition rejects reviewer self-approval and rejects non-compliance checker role before disposition mutation | `npm run scripts:typecheck`, `node --test tests/amlGovernance.test.mjs`, and `AmlFdsGovernanceIntegrationTest` passed locally after fixing the first exposed DTO mismatch | `docs/test-evidence/hardening-h6-aml-fds-governance.md`, `docs/test-evidence/generated/aml-model-card.json`, `docs/test-evidence/generated/synthetic-str-report.json`, `docs/test-evidence/generated/aml-regulatory-report.json`, `docs/test-evidence/generated/aml-str-report-summary.json` | complete |
| hardening-h7 | Synthetic analytical data platform with DuckDB/Parquet marts, lineage, DQ gate, and reproducible risk report | not-applicable | not-applicable; generated data-platform evidence is analytical rather than channel UI | no public channel API contract changes; root `npm run data:dq-check` adds a data-platform gate | not-applicable for Spring API; implemented in the Python/DuckDB analytics package | synthetic CSV OLTP extracts load into DuckDB marts and write Parquet evidence under `docs/test-evidence/generated/data-platform/` | synthetic boundary controls assert no real money, PII, KYC, payment-network, or external-provider data | field lineage resolves source extract fields through marts to report fields; evidence includes source SHA-256 hashes | not-applicable for maker-checker; DQ gate fails before report evidence is accepted when dirty data is injected | `npm run analytics:fds-aml:test`, `npm run data:dq-check`, `node --test tests/dataPlatform.test.mjs`, and an injected-dirty CLI run passed/failed as expected locally | `docs/test-evidence/hardening-h7-data-platform.md`, `docs/test-evidence/generated/data-platform/data-platform-evidence.json`, `docs/test-evidence/generated/data-platform/data-lineage.json`, `docs/test-evidence/generated/data-platform/synthetic-risk-report.json`, `docs/test-evidence/generated/data-platform/*.parquet` | complete |
| hardening-h8 | Regulatory and governance artifact automation | not-applicable | not-applicable; generated governance evidence is an operations/control artifact slice | no public channel API contract changes; root `npm run governance:evidence` adds the generator | not-applicable for Spring API; generator reads checked-in synthetic realm and generated evidence artifacts | not-applicable; no new migration and no ledger/source-row mutation | synthetic Keycloak realm roles are reviewed; deployment approval uses maker/checker separation; no live IAM or regulator integration is claimed | H8 artifacts preserve source hashes, security evidence status history, deployment approval reason, and incident-response timeline | deployment approval maker/checker are separated; access-rights review flags over-privilege combinations for review | `npm run governance:evidence`, `node --test tests/governanceEvidence.test.mjs`, `npm run scripts:typecheck`, and `npm run evidence:refresh-check` passed locally | `docs/test-evidence/hardening-h8-governance-artifacts.md`, `docs/test-evidence/generated/governance/*.json`, `docs/incident-response/synthetic-incident-response-runbook.md`, `docs/regulatory-mapping/governance-artifact-mapping.md` | complete |

## Runtime Coverage Notes

- `core-banking statement-service`: `V034__statement_balance_certificate_snapshots.sql` and `StatementReadModelIntegrationTest` now prove balance certificates persist synthetic snapshot metadata, source posting count, source last business date, source ledger hash, and first/last audit references; repeat reads return the persisted first-view projection values instead of recomputing mutable current available-balance fields.
- `payment-service`: `LivePaymentOutboxWorkerComposeSmokeIntegrationTest` and `npm run test:payment-service:outbox-worker-compose` now prove the live Compose `payment-outbox-worker` settles an API-created synthetic payment through live `core-banking` as balanced `BILL_PAYMENT` postings using its own Keycloak `payment-service-api` client-credentials token with simulator-token fallback disabled, and leaves the core settlement event in durable outbox state.
- `payment-service`: `npm run test:payment-service:keycloak-service-token` now proves the imported Keycloak `payment-service-api` service account can issue a signed `PAYMENT_SERVICE` client-credentials token with `payment-service-api` and `core-banking-api` audiences, and payment-service accepts it on the dispatch route with simulator fallback disabled.
- `payment-service`: `PaymentKafkaOutboxPublisherIntegrationTest` now proves dispatcher-created `PaymentInstructionFailed`, `PaymentInstructionRetryScheduled`, and `PaymentInstructionDeadLettered` rows publish to Redpanda as synthetic-only payment-domain envelopes while the underlying `PaymentLedgerPostingRequested` rows remain on the core-ledger dispatch path in retry or dead-letter state.
- `fds-aml-console`: `ApiBackedRiskPanel` now includes conditional `FDS-301` parameter read and future-effective change-request browser smoke wiring through the Spring parameter API client; local Playwright shell verification passed with the API-backed command test skipped until `BANKING_LAB_E2E_API_BASE_URL` is configured.
- `ops-console`: `ApiBackedOpsPanel` now includes conditional `OPS-301` reconciliation parameter read and future-effective change-request browser smoke wiring through the Spring parameter API client; local Playwright shell verification passed with the API-backed command test skipped until `BANKING_LAB_E2E_API_BASE_URL` is configured.
- `ops-console`: `ApiBackedOpsPanel` now includes conditional `OPS-LEDGER-101/102/103` projection drift, rebuild request approval, and rebuild execution smoke wiring through the Spring API client; `npm run test:e2e -- --grep "projection"` ran locally and skipped the live API smoke until `BANKING_LAB_E2E_API_BASE_URL` is configured, the full ops-console Playwright file passed after the manifest label assertion was scoped to the first match, and the full local Playwright manifest suite passed with live API tests still gated by missing service URLs.
- `audit-console`: `ApiBackedAuditPanel` now includes conditional `AUD-201` audit parameter read and future-effective change-request browser smoke wiring through the Spring parameter API client; local Playwright shell verification passed with the API-backed command test skipped until `BANKING_LAB_E2E_API_BASE_URL` is configured.
- `admin-console`: `ApiBackedAdminPanel` now includes conditional `ADM-201` security parameter read and future-effective change-request browser smoke wiring through the Spring parameter API client; local Playwright shell verification passed with the API-backed command test skipped until `BANKING_LAB_E2E_API_BASE_URL` is configured.
- `admin-console`: `ApiBackedAdminPanel` now includes conditional `ADM-301` authorization parameter read and future-effective change-request browser smoke wiring through the Spring parameter API client; `ParameterAdminService` validates menu-role value shapes before approval creation, and local Playwright shell verification passed with the API-backed command test skipped until `BANKING_LAB_E2E_API_BASE_URL` is configured.
- `notification-service`: `npm run test:notification-service:keycloak-service-token` now proves the imported Keycloak `notification-service-api` service account can issue a signed `NOTIFICATION_SERVICE` client-credentials token with the `notification-service-api` audience, and notification-service accepts it on the event route with simulator fallback disabled while preserving masked synthetic delivery output.
- `notification-service`: `npm run test:notification-service:provider-dead-letter-compose` now proves the live Compose notification API can create a masked pending delivery with a signed `notification-service-api` token, record retryable and terminal synthetic provider failures, write a durable dead-letter row, reject delivered-state mutation after dead-letter, and preserve masking in delivery history.
- `notification-service`: `V004__notification_template_workflow_visibility.sql` and targeted notification integration tests now prove notification template change requests create durable `NOTIFICATION_TEMPLATE_CHANGE` workflow instances, append `REQUESTED` plus terminal approval/rejection events, expose auditor-readable workflow timelines, and reject CUSTOMER access to workflow list state.
- `reporting-service`: `npm run test:reporting-service:keycloak-service-token` now proves the imported Keycloak `reporting-service-api` service account can issue a signed `REPORTING_ANALYST` client-credentials token with the `reporting-service-api` audience, and reporting-service accepts it on catalog, artifact generation, artifact list, artifact export, and retention sweep routes with simulator fallback disabled.
- `reporting-service`: `ADM-701` and `AUD-401` now wire reporting-service metadata APIs into admin-console and audit-console API-backed panels with reason text, simulator-token local smoke support, masked-by-default metadata display, and conditional Playwright coverage when `BANKING_LAB_E2E_REPORTING_API_BASE_URL` is configured.
- `reporting-service`: `V002__report_artifact_rendering.sql`, `V003__report_artifact_retention_lifecycle.sql`, `V004__reporting_outbox_events.sql`, and `ReportingServiceIntegrationTest` now prove generated artifacts persist rendered synthetic JSON content, SHA-256 checksums, `SYNTHETIC_7Y` retention metadata, JSON export format, reason-required package export simulations, metadata-only expiration, and durable `PENDING` outbox events while leaving ledger/source rows unmutated.
- `reporting-service`: `ReportingKafkaOutboxPublisherIntegrationTest` now proves `ReportArtifactGenerated` and `ReportArtifactExported` outbox rows publish to Redpanda with synthetic-only envelopes and transition from `PENDING` to `PUBLISHED` after broker acknowledgement.
- `reporting-service`: Docker Compose, raw Kubernetes, Helm, Prometheus, scaffold, and security posture checks now include a separate `reporting-domain-event-publisher` runtime with the API container publisher mode disabled and the worker publisher mode enabled.
- `reporting-service`: `V005__reporting_outbox_retry_dead_letter.sql` and `ReportingKafkaOutboxPublisherIntegrationTest` now prove broker failures transition reporting outbox rows through retryable `FAILED` state and terminal `DEAD_LETTER` state without marking `published_at`.
- `reporting-service`: `npm run test:reporting-service:domain-publisher-compose` now proves the live Compose `reporting-domain-event-publisher` can restart, pick up a durable pending `ReportArtifactGenerated` outbox row, publish a synthetic-only Redpanda envelope, mark the row `PUBLISHED`, and emit batch log counters.
- `payment-service`, `notification-service`, and `reporting-service`: service-specific Flyway history tables now use shared-schema baseline version `0` so V001+ service migrations can initialize after core-banking has already populated the shared synthetic PostgreSQL `public` schema.

## Baseline Commands

Commands run through 2026-06-06 for this review:

- `npm ci`: pass.
- `npm test`: pass, 158 tests after WRK-003 workflow timeline scaffold coverage.
- `npm run validate:manifests`: pass, 101 manifests after WRK-003 workflow timeline manifest update.
- `npm run test:screen-engine`: pass, 10 tests.
- `npm run packages:typecheck`: pass.
- `npm run scripts:typecheck`: pass.
- `npm run security:posture-check`: pass, 22 secure-default controls verified for Spring, Docker Compose, Kubernetes, Helm, reporting-service, and Flyway migration posture.
- `npm run ledger:integrity-check`: pass after H3 DB-integrity update and sandbox escalation; verifies deferred PostgreSQL trigger rejection, typed system-account routing, and 2026 partition-route child tables.
- `npm run dr:multi-instance-drill`: first run failed with PostgreSQL `could not serialize access` on duplicate idempotency insert across independent Spring contexts; rerun passed after bounded retry fix and generated `docs/test-evidence/generated/ha-dr-multi-instance-drill.json`.
- `npm run siem:alert-drill`: pass; validates synthetic Loki operational-security alert rules and writes `docs/test-evidence/generated/siem-alert-drill.json`.
- `npm run next:staff-terminal:typecheck`: pass.
- `npm run next:staff-terminal:build`: pass.
- `npm run test:e2e -- apps/staff-terminal/e2e/integrated-terminal.spec.ts`: pass after PAY-102 staff cancellation smoke update with 5 passed and 18 skipped because API/payment/Keycloak variables were not configured.
- `npm run test:e2e`: pass after Phase C fee policy/posting update with 17 passed and 41 skipped because API/Keycloak variables were not configured.
- `docker compose config`: pass.
- `docker compose --profile platform config`: pass.
- `npm run test:core-banking:unit -- --rerun-tasks`: pass after sandbox escalation.
- `scripts/run-core-banking-tests.sh :services:core-banking:compileKotlin :services:core-banking:compileIntegrationTestKotlin`: pass after sandbox escalation.
- `npm run test:core-banking:integration -- --tests lab.banking.core.security.SecurityAuthorizationIntegrationTest --tests lab.banking.core.staff.StaffAccessApiParityIntegrationTest --rerun-tasks`: pass after WRK-002 operational retry queue update.
- `npm run test:core-banking:integration -- --tests lab.banking.core.staff.StaffAccessApiParityIntegrationTest --rerun-tasks`: pass after WRK-003 workflow timeline update.
- `npm run test:core-banking:integration -- --tests lab.banking.core.product.DepositProductApiIntegrationTest`: pass after Phase C product-interest update.
- `npm run test:core-banking:integration -- --tests lab.banking.core.product.FeePolicyApiIntegrationTest --rerun-tasks`: pass after Phase C fee policy/posting update.
- `npm run test:core-banking:integration -- --tests lab.banking.core.eod.EodClosingPipelineIntegrationTest`: pass after Phase 1B EOD pipeline update and sandbox escalation for Gradle file-lock socket access.
- `npm run test:core-banking:integration -- --tests lab.banking.core.statement.StatementReadModelIntegrationTest`: pass after Phase 1C statement/certificate read-model update and sandbox escalation for Gradle file-lock socket access.
- `npm run test:core-banking:integration -- --tests lab.banking.core.loan.LoanDomainIntegrationTest`: pass after Phase 2 loan-domain update and sandbox escalation for Gradle file-lock socket access; first escalated run exposed an integration-test Flyway path issue, which was fixed before rerun.
- `npm run test:core-banking:integration -- --tests lab.banking.core.card.CardDomainIntegrationTest`: pass after Phase 3 card-domain update and sandbox escalation for Gradle file-lock socket access; first card run exposed that the over-limit assertion was hidden by 3DS validation, which was fixed before rerun.
- `npm run test:core-banking:integration -- --tests lab.banking.core.parameters.ParameterAdminIntegrationTest`: pass after Phase 4 parameter-admin update and sandbox escalation for Gradle file-lock socket access; earlier runs exposed Kotlin visibility, account-limit fixture, and reset-cascade issues, all fixed before rerun. Targeted rerun with `--rerun-tasks` passed after adding structured `ADM-301` value validation.
- `npm run test:parameter-command-e2e-compose`: pass after adding the live Spring API wrapper for FDS/OPS/AUD/ADM parameter command browser smokes; first wrapper attempts exposed ADM-301 readiness and route-role preflight issues, both fixed before the passing rerun.
- `npm run test:core-banking:integration -- --tests lab.banking.core.analytics.FdsAnalyticsEvidenceIntegrationTest`: pass after Phase 5 FDS analytics Spring read API update and sandbox escalation for Gradle file-lock socket access.
- `npm run test:payment-service:unit`: first sandboxed Gradle run failed with `java.net.SocketException: Operation not permitted`; rerun after sandbox escalation passed and compiled the new payment-service module with no unit test sources.
- `npm run test:payment-service:integration`: first sandboxed Gradle run failed with `java.net.SocketException: Operation not permitted`; rerun after sandbox escalation passed `PaymentInstructionIntegrationTest` against PostgreSQL Testcontainers.
- `npm run test:notification-service:integration -- --rerun-tasks`: pass; passed notification delivery/history, authorization, template admin, preference filtering, Kafka consumer, and live Compose smoke integration coverage against PostgreSQL/Redpanda Testcontainers as configured.
- `npm run test:notification-service:integration -- --tests lab.banking.notification.NotificationAuthorizationIntegrationTest --rerun-tasks`: pass after customer notification preference and delivery-history self-service route updates; matching CUSTOMER scope can list/upsert owned preferences, list owned masked deliveries, and cross-customer/ops calls are rejected.
- `npm run test:notification-service:integration -- --tests lab.banking.notification.NotificationTemplateAdminIntegrationTest --tests lab.banking.notification.NotificationAuthorizationIntegrationTest --rerun-tasks`: pass after notification template workflow visibility update; sandboxed Gradle run failed with file-lock socket denial, and approved escalated rerun passed.
- `npm run test:notification-service:unit -- --rerun-tasks`: pass; compiled the notification-service module and ran `NotificationEventConsumerWorkerTest`.
- `npm run test:notification-service:provider-dead-letter-compose`: pass after notification provider dead-letter live Compose smoke update; PostgreSQL, Redpanda, Keycloak, and `notification-service` started under an isolated Compose project, a masked pending delivery transitioned through `FAILED` to `DEAD_LETTER`, one durable dead-letter row was verified, and delivered-state mutation returned structured `NOTIFICATION_STATE_TRANSITION_REJECTED`.
- `npm run analytics:fds-aml:test`: pass, 10 Python tests for DuckDB feature generation, rules, scoring artifact, data-platform DQ, lineage, and reproducible reporting.
- `npm run analytics:fds-aml`: pass, generated `docs/test-evidence/generated/fds-aml-analytics.json` and `.csv`.
- `npm run test:core-banking:integration -- --tests lab.banking.core.security.SecurityDefaultsIntegrationTest --tests lab.banking.core.security.JwksAuthorizationIntegrationTest --rerun-tasks`: pass after H2 secure-auth update and sandbox escalation; first run exposed a missing FDS parameter seed for the transfer success path, which was fixed before rerun.
- `npm run test:core-banking:integration -- --rerun-tasks`: pass after H2 secure-auth update; first full run exposed integration-test actuator exposure shadowing, which was fixed before rerun.
- `npm run test:core-banking:integration -- --tests lab.banking.core.ledger.application.LedgerDatabaseIntegrityIntegrationTest --tests lab.banking.core.product.DepositProductApiIntegrationTest --tests lab.banking.core.product.FeePolicyApiIntegrationTest --tests lab.banking.core.loan.LoanDomainIntegrationTest --tests lab.banking.core.card.CardDomainIntegrationTest --rerun-tasks`: pass after H3 DB-integrity update and fixture transaction-boundary fix.
- `npm run test:core-banking:integration -- --tests lab.banking.core.statement.StatementReadModelIntegrationTest --tests lab.banking.core.fds.FdsCaseApiParityIntegrationTest --tests lab.banking.core.reconciliation.ReconciliationOpsApiParityIntegrationTest --tests lab.banking.core.eod.EodClosingPipelineIntegrationTest --tests lab.banking.core.parameters.ParameterAdminIntegrationTest --tests lab.banking.core.staff.StaffAccessApiParityIntegrationTest --rerun-tasks`: pass after H3 DB-integrity update and fixture transaction-boundary fix.
- `npm run formal:ledger`: pass, actual TLC ran through the supplied `~/Downloads/tla2tools.jar` for `formal/Ledger.tla` and `formal/Idempotency.tla`; the bounded state-search checker also covered 3335 states, 8241 transitions, and 15 invariants including posting-time limit usage.
- `npm run test:core-banking:integration -- --tests lab.banking.core.ledger.application.LedgerCommandServiceIntegrationTest`: pass after Phase 1A limit enforcement update; initial sandboxed Gradle run failed with `java.net.SocketException: Operation not permitted`, then passed outside the sandbox.
- `npm run platform:validate`: pass after TLS Ingress, Helm Keycloak/Redpanda/Temporal resources, and Argo CD validation were added.
- `npm run k8s:validate`: pass; structural validation covered 27 Kubernetes resources and the latest local run recorded `kubectlClientDryRun.status=skipped_no_cluster`.
- `npm run helm:template`: pass with Helm CLI renderer and 27 rendered resources.
- `npm run argocd:validate`: pass for 2 Argo CD Application manifests.
- `kind create cluster --name banking-lab-evidence --wait 120s`: pass; disposable cluster reached Ready and was deleted after the run.
- `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./gradlew :services:core-banking:bootJar`: pass after the default-JDK attempt failed because JDK 26.0.1 is not supported by the current Gradle/Kotlin DSL.
- `docker build -f infra/docker-compose/core-banking.Dockerfile -t banking-lab/core-banking-service:local .`: pass using the freshly regenerated `core-banking-0.1.0-migration.jar` artifact.
- `helm install banking-lab infra/helm/banking-lab --namespace banking-lab --create-namespace --set temporalWorker.replicas=0 --wait --timeout 5m`: pass; PostgreSQL and core-banking reached `1/1` Ready.
- `kubectl get --raw /api/v1/namespaces/banking-lab/services/http:banking-lab-core-banking:8081/proxy/health`: pass with `status=ok`, `syntheticOnly=true`, `auditHashChainValid=true`, and `migrationTarget=kotlin-spring-boot`.
- `kubectl apply --server-side --dry-run=server --force-conflicts -f infra/k8s`: previous live cluster run passed for 15 raw Kubernetes resources after an initial non-forced dry-run exposed Helm-owned Secret field-manager conflicts; current 23-resource payment, reporting, and notification expansion is structurally validated but not live-cluster dry-run.
- `npm run load:synthetic`: pass, 74 local synthetic requests across 54 scenario runs; ledger invariant and audit hash-chain checks passed.
- `npm run next:fds-aml-console:typecheck`: pass after the analytics panel switched from direct JSON import to `fdsAnalyticsEvidence(...)`.
- `npm run next:admin-console:typecheck`: pass after the reporting artifact management API-backed panel update.
- `npm run next:audit-console:typecheck`: pass after the reporting artifact history API-backed panel update.
- `npm run next:customer-web:typecheck`: pass after the notification preference self-service API-backed panel update.
- `npm run test:core-banking:integration -- --tests lab.banking.core.statement.StatementReadModelIntegrationTest --rerun-tasks`: pass after statement balance certificate snapshot update; PostgreSQL Testcontainers verified persisted `balance_certificate_snapshots`, source posting metadata, deterministic certificate IDs, and first-view snapshot reuse after mutable projection changes.
- `npm run test:e2e -- apps/admin-console/e2e/admin-console-parity.spec.ts apps/audit-console/e2e/audit-console-parity.spec.ts`: pass after the reporting manifest update; 4 manifest-rendering tests passed and 6 API/Keycloak/reporting live smokes skipped because corresponding E2E base URLs were not configured.
- `npm run test:reporting-service:integration -- --tests lab.banking.reporting.ReportingServiceIntegrationTest --rerun-tasks`: sandboxed run failed before tests with Gradle `FileLockContentionHandler` socket denial; rerun after sandbox escalation passed with rendered artifact payload, checksum, and retention metadata assertions.
- `npm run test:reporting-service:integration -- --tests lab.banking.reporting.ReportingKafkaOutboxPublisherIntegrationTest --rerun-tasks`: pass after reporting Kafka publisher update; Redpanda Testcontainers consumed generated/exported reporting envelopes, verified outbox rows reached `PUBLISHED`, and covered retry delay plus `DEAD_LETTER` transition on broker failure.
- `npm run test:reporting-service:domain-publisher-compose`: pass after reporting publisher live Compose smoke update; PostgreSQL, Redpanda, and `reporting-domain-event-publisher` started under an isolated Compose project, a synthetic pending report outbox event was published to Redpanda after worker restart, and the row reached `PUBLISHED`.
- `node --test tests/reportingServiceScaffold.test.mjs tests/nextScaffold.test.mjs`: pass with 16 tests after reporting rendered-artifact scaffold and channel metadata updates.
- `npm run test:reporting-service:keycloak-service-token`: pass after reporting retention route update; live synthetic Compose smoke proved Keycloak `reporting-service-api` client-credentials token can catalog, generate, list, export a rendered artifact, and run a no-op retention sweep with simulator tokens disabled.
- `npm run test:e2e -- apps/customer-web/e2e/customer-web-parity.spec.ts`: pass after the notification preference manifest update; manifest-rendering smoke passed and live API/Keycloak/payment/notification smokes skipped because corresponding E2E base URLs were not configured.
- `npm run next:complaint-portal:typecheck`: pass after the complaint self-service material/reopen/type-guide panel update.
- `npm run test:core-banking:integration -- --tests lab.banking.core.complaint.CustomerComplaintSelfServiceApiIntegrationTest --rerun-tasks`: sandboxed run failed with `java.net.SocketException: Operation not permitted`; rerun after sandbox escalation passed against PostgreSQL Testcontainers.
- `npm run test:core-banking:integration -- --tests 'lab.banking.core.complaint.*' --rerun-tasks`: pass after sandbox escalation for complaint entry, answer approval, confirmation, and self-service extension tests.
- `npm run test:e2e -- apps/complaint-portal/e2e/complaint-portal-parity.spec.ts`: pass after the complaint self-service update; 2 manifest/shell tests passed and 5 API/Keycloak live smokes skipped because API/Keycloak E2E base URLs were not configured.
- `env BANKING_LAB_DAST_URL=http://host.docker.internal:18132/health npm run security:evidence:docker`: pass after sandbox escalation against a disposable synthetic core-banking target; npm audit, Docker-forced Semgrep, Trivy, CycloneDX SBOM, and ZAP baseline all passed with 0 skipped.
- `npm run security:evidence`: pass after H2 sandbox escalation; npm audit, Semgrep, Trivy, and CycloneDX SBOM passed, while DAST was explicitly skipped because `BANKING_LAB_DAST_URL` was not set.
- `npm run governance:evidence`: pass; generated access-rights review, deployment approval, incident-response drill log, vulnerability-remediation tracker, governance summary, and synthetic incident-response runbook.
- `npm run postgres:backup-drill`: pass in fixture mode after adding `account_balance_projections == Σ signed postings` and customer available-balance checks to the drill contract.
- `npm run postgres:backup-drill:docker-live`: pass after sandbox escalation; started disposable Compose `postgres:16-alpine` source/restore databases, applied migrations, seeded synthetic balanced rows, invoked canonical `npm run postgres:backup-drill -- --mode=live`, and generated `docs/test-evidence/generated/postgres-backup-restore-drill.json` with `postgresLive=true`. H4 rerun after V027 restored 10 balance projections due synthetic system accounts.
- `npm run evidence:refresh-check`: pass.
- `npm run retirement:audit`: pass.
- `npm run retirement:final-review:verify`: pass.
- `npm run node:retirement-gate`: pass.
- `node --test tests/stackRetirementAreaAudit.test.mjs`: pass after H1 hardening; includes an in-memory canary proving `npm run node:retirement-gate` rejects target-path imports of `runtime/synthetic-reference`.
- `npm run goal:completion-audit -- --require-complete`: pass for the previous Node retirement scope; the missing-features goal is tracked by this matrix and the phase evidence listed above.

## Immediate Implementation Plan

1. Phase B staff command depth now covers account hold/release, transfer limit change, KYC review, fee waiver, and transaction correction with migration, Spring service/controller, api-client method, manifest wiring, staff-terminal panel, passing StaffAccess integration test, and conditional Playwright smoke.
2. Phase C now covers deposit products, rate versioning, interest accrual/posting, fee policy versioning, fee posting, and targeted fee refund reversal through approved fee waivers.
3. Phase D/5 analytics now has DuckDB batch evidence, a Spring `GET /api/fds/analytics` read API, and an FDS/AML console panel that reads through the shared API client instead of importing the file directly.
4. Phase E now has actual TLC evidence for both formal models plus executable bounded state-search evidence with static-only disallowed in CI.
5. Phase F now has Kubernetes/Helm structure, executable structural validation, reporting-service structural deployment coverage, and disposable kind/Helm live deployment smoke evidence for PostgreSQL plus core-banking. Phase G now has local synthetic load-smoke evidence plus live PostgreSQL backup/restore evidence from disposable Compose source/restore databases.
