export default function CallCenterApiSimulatorPage() {
  return (
    <main className="channel-shell channel-app-call-center-console">
      <section className="channel-panel" aria-labelledby="call-center-api-simulator-title">
        <p className="channel-eyebrow">Lab only</p>
        <h1 id="call-center-api-simulator-title">Call-center API simulator</h1>
        <p>Synthetic bearer-token and raw workflow smoke controls are isolated from the agent workspace.</p>
        <a href="/lab/evidence">Open API evidence controls</a>
      </section>
    </main>
  );
}
