export default function StaffManifestBoundaryPage() {
  return (
    <main className="iworks-root staff-lab-root">
      <section className="staff-lab-page" aria-labelledby="staff-manifest-title">
        <p>LAB_ONLY · boundary record</p>
        <h1 id="staff-manifest-title">Staff screen registry</h1>
        <p>The integrated terminal uses its typed transaction-code registry. No standalone staff manifest renderer is active.</p>
      </section>
    </main>
  );
}
