apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-notification-service
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/part-of: banking-lab
    app.kubernetes.io/name: {{ .Release.Name }}-notification-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}-notification-service
  ports:
    - name: http
      port: {{ .Values.notificationService.port }}
      targetPort: http
