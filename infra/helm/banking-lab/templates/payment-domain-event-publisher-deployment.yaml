apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-payment-domain-event-publisher
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/part-of: banking-lab
    app.kubernetes.io/name: {{ .Release.Name }}-payment-domain-event-publisher
spec:
  replicas: {{ .Values.paymentService.domainEventPublisherReplicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}-payment-domain-event-publisher
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: banking-lab
        app.kubernetes.io/name: {{ .Release.Name }}-payment-domain-event-publisher
    spec:
      containers:
        - name: payment-domain-event-publisher
          image: {{ .Values.paymentService.image | quote }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: {{ .Values.paymentService.port }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-config
            - secretRef:
                name: {{ .Release.Name }}-secret
          env:
            - name: PORT
              value: {{ .Values.paymentService.port | quote }}
            - name: BANKING_LAB_SYNTHETIC_ONLY
              value: {{ .Values.syntheticOnly | quote }}
            - name: BANKING_LAB_PAYMENT_CORE_BANKING_HTTP_ENABLED
              value: "true"
            - name: BANKING_LAB_CORE_BANKING_BASE_URL
              value: "http://{{ .Release.Name }}-core-banking:{{ .Values.coreBanking.port }}"
            - name: BANKING_LAB_PAYMENT_CORE_BANKING_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secret
                  key: PAYMENT_CORE_BANKING_SERVICE_TOKEN
            - name: BANKING_LAB_PAYMENT_OUTBOX_WORKER_ENABLED
              value: "false"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_BOOTSTRAP_SERVERS
              value: {{ .Values.paymentService.domainEventPublisherBootstrapServers | quote }}
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_TOPIC
              value: {{ .Values.paymentService.domainEventPublisherTopic | quote }}
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_ENABLED
              value: "true"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_CLIENT_ID
              value: {{ .Values.paymentService.domainEventPublisherClientId | quote }}
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_BATCH_SIZE
              value: "100"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_POLL_INTERVAL_MILLIS
              value: "1000"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_TIMEOUT_MILLIS
              value: "5000"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_DEAD_LETTER_THRESHOLD
              value: "3"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_RETRY_DELAY_SECONDS
              value: "60"
            - name: BANKING_LAB_PAYMENT_DOMAIN_EVENT_PUBLISHER_EVENT_TYPES
              value: {{ .Values.paymentService.domainEventPublisherEventTypes | quote }}
            - name: BANKING_LAB_OTEL_SERVICE_NAME
              value: "banking-lab-payment-domain-event-publisher"
            - name: BANKING_LAB_SECURITY_AUDIENCE
              value: {{ .Values.paymentService.securityAudience | quote }}
            - name: BANKING_LAB_PAYMENT_DATABASE_URL
              value: "jdbc:postgresql://{{ .Release.Name }}-postgres:5432/{{ .Values.postgres.database }}"
            - name: BANKING_LAB_PAYMENT_DATABASE_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secret
                  key: SPRING_DATASOURCE_USERNAME
            - name: BANKING_LAB_PAYMENT_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secret
                  key: SPRING_DATASOURCE_PASSWORD
            - name: SPRING_FLYWAY_TABLE
              value: "payment_flyway_schema_history"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: {{ .Values.paymentService.workerResources.requests.cpu | quote }}
              memory: {{ .Values.paymentService.workerResources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.paymentService.workerResources.limits.cpu | quote }}
              memory: {{ .Values.paymentService.workerResources.limits.memory | quote }}
